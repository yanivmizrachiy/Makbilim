# SPEC — מקור האמת היחיד של Makbilim

> מסמך זה הוא החוזה הקנוני היחיד של הפרויקט **„זוויות בין ישרים מקבילים”**. כל סוכן AI, מפתח, קוד, JSON, workflow, preview, PDF ובדיקה חייבים לפעול לפיו. במקרה של סתירה — `SPEC.md` גובר.

---

# 0. סדר עדיפויות וכללי הכרעה

## 0.1 היררכיית החלטות
1. דיוק מתמטי.
2. רצף דידקטי והתקדמות אמיתית בלמידה.
3. התאמה לסגנון ולארכיטיפים של חומרי המקור.
4. בהירות לתלמיד בעברית תקינה.
5. איכות שרטוט, עימוד והדפסה.
6. יעילות טכנית ותחזוקתיות.

אין להקריב סעיף גבוה לטובת סעיף נמוך.

## 0.2 מקור אמת יחיד
- `SPEC.md` הוא מקור הדרישות היחיד.
- לכל עובדה מבנית יש מקור מכונה אחד: מדיניות המספור, סדר העמודים והכותרות — `src/content/booklet-pages.json`; מספר בלוקי תוכנית הלימודים וסדרם — `sources/curriculum/bbb-parallel-lines.manifest.json`; סדר השאלות המחוברות ומספרן (`originalTaskCount`, הנעילה היחידה נגד מחיקת שאלה) — `src/content/question-plan.json`; ערכי עיצוב — `src/styles/tokens.css`. כל מונה, רשימה או ציפייה בסקריפט, בבדיקה או במסמך נגזרים משם ואינם מצוטטים מחדש.
- README, קוד, JSON וקבצי תכנון הם מימושים בלבד ואינם רשאים להוסיף דרישות עצמאיות.
- כל דרישה חדשה של המשתמש מנורמלת תחילה לניסוח מקצועי, חד־משמעי וניתן לבדיקה בתוך מסמך זה.
- אין לשמור דרישות כתמלול שיחה כאשר ניתן לנסחן כחוזה מוצר/מימוש ברור.

## 0.3 טיפול באי־ודאות
אם חסר פרט מהותי שאי־אפשר להסיק בבטחה מן ה־SPEC, מן המקורות או מן המימוש הקיים — אין להמציא. יש לבקש החלטה מהמשתמש.

---

# 1. מטרת המוצר והתוצר הסופי

יש ליצור חוברת לימוד והערכה אמיתית לחטיבת הביניים בנושא **זוויות בין ישרים מקבילים**, ברמת ספר לימוד מקצועי.

התוצר הסופי חייב להיות:
- מדויק מתמטית.
- מדורג מן הבסיס אל המתקדם.
- מגוון ללא חזרות מכניות.
- כתוב בעברית תקינה ובסגנון דפי עבודה ומבחנים בישראל.
- מבוסס שרטוטים וקטוריים מדויקים ואחידים.
- מעוצב באחידות גבוהה כמו ספר לימוד אחד.
- מוכן להדפסת A4 בצבע ובשחור־לבן.
- ללא placeholder, demo copy, הערות פיתוח או טקסט AI שאינו מיועד לתלמיד.

**מבנה מול התלמיד — נושאים, לא יחידות.** מבנה „יחידה 1..5” מבוטל מן המוצר הפונה לתלמיד. אין להציג „יחידה” בשום כותרת, running head, מספור או תווית. החוברת מאורגנת לפי כותרות נושא טבעיות (ראו 4.3), כאשר **„שאלות מתוך תוכנית הלימודים” פותחות את החוברת**. חלוקה פנימית בקוד (למשל `questions-unit*.ts`) ומזהי משימה הם ארטיפקט מימוש שנשמר ב־DOM/בקוד בלבד ואינו מוצג.

**מקור התוכן הדידקטי מול תוכנית הלימודים.** הנושאים הדידקטיים הם חומר מקורי שנבנה בפרויקט; בלוקי „שאלות מתוך תוכנית הלימודים” הם snapshot נאמן משאלות תוכנית הלימודים שאותרו בריפו `yanivmizrachiy/bbb`. מספר העמודים והמשימות נקבע לפי איכות, הדרגתיות וניצול נכון של A4; אין לנפח כמות במחיר איכות.

**החלטות אחרונות גוברות.** מסמך זה מגלם את ההחלטה לעבור למבנה נושאים, למספור עמודים רציף (1..N), ולהקדמת שאלות תוכנית הלימודים. כל דרישה ישנה שסותרת החלטות אלה בטלה.

---

# 2. Corpus המקורות — בסיס לשפה, סוגי משימות ומורכבות

חומרי המקור שסופקו או נאספו לפרויקט הם **Corpus ייחוס פדגוגי ולשוני**.

## 2.1 מה לומדים מן המקורות
- פעלי הוראה וניסוח מקובל.
- סוגי שאלות ופורמטים.
- אורך וקצב של הוראה.
- רמת פירוט של נתונים.
- מבני שרטוט.
- שיפוע וכיוון של ישרים וחותכים.
- רמות מורכבות וקושי.
- סוגי תשובות ומרחבי כתיבה.
- רצף בין זיהוי, חישוב, אלגברה, נימוק והוכחה.

## 2.2 שימוש מותר
- חומר שהמשתמש רשאי לעבד: ניתן לבצע adaptation משמעותי.
- מקור חיצוני: לומדים ארכיטיפ, register ומיומנות ויוצרים שאלה מקורית.
- חריג: בלוק „שאלות מתוך תוכנית הלימודים” אינו adaptation. הוא snapshot נאמן של שאלות תוכנית הלימודים שאותרו ב־`yanivmizrachiy/bbb`.

## 2.3 שימוש אסור
- אין להעתיק שאלה חיצונית בשלמותה בנושאים הדידקטיים.
- אין ליצור העתקה מוסווית באמצעות שינוי מספרים/אותיות בלבד בנושאים הדידקטיים.
- אין לשכפל שרטוט חיצוני זהה בנושאים הדידקטיים כאשר ניתן ליצור תצורה מקורית.
- בבלוקי תוכנית הלימודים אסור לשכתב, לקצר, לשפר ניסוח, להחליף מספרים, אותיות, סדר סעיפים או תוכן שרטוט של שאלת המקור. **החריג היחיד המותר בתצוגה** הוא נורמליזציה של הביטוי המדויק „בין מקבילים” לניסוח „בין ישרים מקבילים” (ראו 8.4, 9, 14). כל שינוי אחר אסור, וטקסט המקור נשמר verbatim ב־provenance.

## 2.4 תיעוד מקורות
```text
sources/
├─ references/
├─ curriculum/
│  └─ bbb-parallel-lines.manifest.json
└─ manifest.json
```

לכל משימה מקורית נשמר `sourceArchetypeRefs` פנימי. `sources/` הוא corpus ייחוס ולא מקור דרישות נוסף. לבלוק תוכנית הלימודים קובץ ה־manifest משמש אינדקס מקור בלבד; הדרישה לנאמנות נמצאת במסמך זה.

---

# 3. התוכן המתמטי הקנוני

## 3.1 משפטים ישירים

הישר החותך יוצר עם שני הישרים **שמונה זוויות** (ארבע בכל נקודת חיתוך). יש להדגיש ולהראות בפועל את שמונה הזוויות, ומתוכן נגזרים זוגות הזוויות: מתאימות, מתחלפות וחד-צדדיות. „מתאימות” הן זוויות באותו צד של החותך ובאותו צד של הישרים; „מתחלפות” הן זוויות בצדדים שונים של החותך ובצדדים שונים של הישרים; „חד-צדדיות” הן זוויות פנימיות באותו צד של החותך.

המשפטים הישירים הקנוניים:
1. **זוויות מתאימות בין ישרים מקבילים שוות.**
2. **זוויות מתחלפות בין ישרים מקבילים שוות.**
3. **זוויות חד-צדדיות בין ישרים מקבילים משלימות ל־180°.**

הניסוח הפורמלי המלא (תנאי ← מסקנה) של המשפטים הישירים, שממנו נגזר הניסוח הקנוני הקצר המופיע בדפי התלמיד:
- אם שני ישרים מקבילים נחתכים על ידי ישר שלישי, אז כל זוג זוויות מתאימות שוות זו לזו.
- אם שני ישרים מקבילים נחתכים על ידי ישר שלישי, אז כל זוג זוויות מתחלפות שוות זו לזו.
- אם שני ישרים מקבילים נחתכים על ידי ישר שלישי, אז כל זוג זוויות חד-צדדיות משלימות ל־180°.

הניסוח מול התלמיד הוא תמיד „בין ישרים מקבילים”, לעולם לא „בין מקבילים”.

### 3.1.1 ניסוח ותרגול המשפטים הישירים
- אלה הם הניסוחים הקנוניים שבהם משתמשים בדפי התלמיד: **„זוויות מתאימות בין ישרים מקבילים שוות”**, **„זוויות מתחלפות בין ישרים מקבילים שוות”** ו־**„זוויות חד-צדדיות בין ישרים מקבילים משלימות ל־180°”**.
- כאשר מתחילים להשתמש במשפטים, משימות ההשלמה הראשונות משלימות **מילה אחת בלבד בכל שורה**. יש לגוון את המילה החסרה, למשל: „מתאימות”, „מתחלפות”, „חד-צדדיות”, „מקבילים”, „שוות”, „180°”.
- שלושת המשפטים הישירים נכונים, ולכן שורה שבה חסר שם סוג הזוויות („זוויות ______ בין ישרים מקבילים שוות.”) חייבת להיות חד־משמעית: בשרטוט הנלווה מסומן זוג זוויות אחד מן הסוג הנדרש, וההוראה מפנה אליו — התלמיד מזהה את הזוג כדי להשלים את המשפט. **חריג:** כאשר זיהוי סוג הזוויות הוא המיומנות הנבדקת, אין לחשוף את הסוג בכותרת, בתווית או בשרטוט (ראו 6, 11.5).
- כל ניסוח קנוני של משפט (ישיר, ניסוח פורמלי מלא והפוך) נלקח בקוד מ־`src/content/theorems.ts` ולא מוקלד מחדש, כך שאינו יכול לסטות.
- אין ליצור משימת השלמה שבה חסרות שתי מילים או יותר באותו משפט.
- לפני חישוב יש רצף מחייב של זיהוי: זיהוי מתאימות/מתחלפות, בדיקה אם הישרים מקבילים, ורק אז החלטה אם מותר להסיק שוויון.
- חייבות להופיע דוגמאות נגד שבהן הזוויות מתאימות או מתחלפות אך הישרים אינם מקבילים, ולכן אין להסיק שהזוויות שוות.
- חייבות להופיע שאלות ביקורתיות בדיאלוג בין נערים ונערות, שבהן תלמיד/ה אומר/ת ניסוח חסר כגון „זוויות מתחלפות שוות”, ותלמיד/ה אחר/ת מתקנ/ת שהשוויון מובטח רק בין ישרים מקבילים.
- רצף הלמידה המחייב הוא: **השלמת משפט במילה אחת → זיהוי → הבחנה וביקורת → חישוב מספרי ללא נעלם → חישוב רב־שלבי ללא נעלם → אלגברה**.
- אין למחוק משימות קיימות כדי לעמוד בדרישות אלה; יש לשמר את 62 המשימות המקוריות ולסנכרן את תוכנן וסדרן.

## 3.2 משפטים הפוכים
4. **אם שני ישרים נחתכים על ידי ישר שלישי, וזוג זוויות מתאימות שוות זו לזו, אז שני הישרים מקבילים.**
5. **אם שני ישרים נחתכים על ידי ישר שלישי, וזוג זוויות מתחלפות שוות זו לזו, אז שני הישרים מקבילים.**
6. **אם שני ישרים נחתכים על ידי ישר שלישי, וזוג זוויות חד-צדדיות משלימות ל־180°, אז שני הישרים מקבילים.**

המשפטים ההפוכים נלמדים ב**פיגום מדורג** ולא כ„הוכח מאפס” בשלב מוקדם. סולם הפיגום: (א) השלמת מילה/שורה אחת בניסוח ההפוך; (ב) דוגמה מספרית קלה; (ג) מתמטיקה חלקית שהתלמיד משלים; (ד) חץ מסקנה ⇓ משותף (11.14); (ה) מסקנת המקבילות. חובה לכלול **דוגמת 112° מודרכת** עם נימוק הפוך נכון מתמטית: נתונות שתי זוויות ששוות זו לזו וגודלן 112°; התלמיד מזהה אם הן מתאימות או מתחלפות, משלים את הנימוק ההפוך המתאים, ומתחתיו — בעזרת החץ ⇓ — מסיק „הישרים מקבילים”. אין לכתוב נימוק שגוי כגון „אם שתי זוויות שוות אז הישרים מקבילים”: השוויון לבדו אינו מספיק, נדרש שהזוג יהיה מתאים/מתחלף ביחס לישרים ולחותך.

## 3.3 הבחנה מושגית שחייבת להילמד
- “מתאימות” ו“מתחלפות” מתארות **מיקום** של זוויות; הן אינן מבטיחות שוויון.
- “זוויות מתחלפות שוות” אינו משפט תקף ללא תנאי המקבילות.
- “זוויות מתאימות שוות” אינו משפט תקף ללא תנאי המקבילות.
- זוויות חד-צדדיות **משלימות ל־180°** (אינן שוות) — רק בין ישרים מקבילים.
- החוברת חייבת לכלול משימות שמזהות ומתקנות את התפיסות השגויות האלה, כולל שרטוטים שבהם הישרים אינם מקבילים.

## 3.4 ידע עזר
זוויות קודקודיות, צמודות וזווית שטוחה מותרות ככלי עזר בתוך שאלות על ישרים מקבילים; הן אינן נושא עצמאי של החוברת. הן משמשות בין היתר להסביר מדוע זוויות חד-צדדיות בין ישרים מקבילים משלימות ל־180° (שוויון זוויות מתאימות + השלמת שתי זוויות צמודות ל־180°).

## 3.5 הגדרות ישרים מקבילים וקטעים מקבילים
- החוברת מלמדת את **הגדרת שני ישרים מקבילים**: „ישרים מקבילים הם ישרים במישור שאין להם נקודות משותפות.”
- ואת **הגדרת שני קטעים מקבילים**: „שני קטעים נקראים מקבילים אם הם נמצאים על ישרים מקבילים.”
- ההגדרות נלמדות בטקסט קצר ותקין, ומותר לתרגלן במשימת השלמה (cloze) עם **בנק מילים** אופציונלי; אין להפוך את כל החוברת לתרגילי מחסן מילים.
- ההבחנה בין „ישר”, „קטע” ו„חותך” נשמרת עקבית לאורך החוברת, ומופיעים בה גם קטעים מקבילים ולא רק ישרים.

---

# 4. ארכיטקטורה דידקטית — נושאים לפי סדר הלמידה

## 4.1 מספור עמודים
- **מספור עמודים רציף יחיד 1..N לכל אורך החוברת, ללא איפוס.** אין מספור מקומי לנושא ואין „עמוד 1” חוזר.
- מספר העמוד מוצג כ**מספר-בלבד בתוך עיגול בפינה השמאלית-עליונה** של הדף (העיקרון מ־`yanivmizrachiy/razpages` — עיגול ומספר בלבד, לא המראה המלא שלו). העיגול עדין ואינו מתחרה בתוכן.
- כותרת הדף היא שם החוברת (נושא החוברת: „זוויות בין ישרים מקבילים”). אין „יחידה N” ואין כותרת נושא/תווית מטא מול התלמיד (ראו 4.3, 11.5).

## 4.2 מספור שאלות והיררכיה
- **רק העמודים ממוספרים** (4.1). **שאלות אינן ממוספרות.** כל שאלה ראשית נפתחת ב**נקודה שחורה מלאה גדולה (●)** בראש השאלה, בצבע הדיו (--ink), במיקום, גודל ויישור קבועים מ־design tokens.
- **סעיפים בתוך שאלה** נפתחים ב**נקודה מלאה קטנה יותר (•)**. אין אותיות סעיף ואין מספרי סעיף.
- **כלל ברזל — אין דמו:** אין מספרי שאלות, אין אותיות סעיף, ואין תוויות מטא (סוג משימה / „זיהוי” / „השלמת משפט” / „בדיקת טענה” וכו') בראש שאלה או סעיף. מזהי משימה/יחידה פנימיים נשמרים ב־DOM כ־`data-task-id` / `data-task-kind` לתחזוקה ולמנגנוני האיכות בלבד, ולעולם אינם מוצגים לתלמיד.
- **בלוקי תוכנית הלימודים:** סימן ה־● בראש כל שאלה ומספר העמוד הם chrome של החוברת (כמו מספרי עמוד) ואינם נחשבים שינוי תוכן. מבנה המקור הפנימי של השאלה (סעיפים/אותיות מקוריים) נשמר כפי שהוא בתוך גוף השאלה.

## 4.3 מבנה הנושאים (סדר החוברת)
הסדר המודפס, כותרות הנושא המדויקות והעמודים מוגדרים במקום אחד בלבד: `src/content/booklet-pages.json`.
כותרות הנושא הן **מבנה פנימי בלבד** — הן קובעות את הסידור, את קיבוץ העמודים במדריך המורה ואת סדר הלמידה, אך **אינן מודפסות בכותרת דף התלמיד** (כלל הברזל „אין דמו”, 11.5). כותרת הדף היא שם החוברת בלבד.
`booklet.ts` (רינדור ומספור), סקריפט ה־QA של ה־PDF, שערי הולידציה והבדיקות נגזרים ממנו ואינם מציינים
מחדש סדר, כותרת או מספר עמודים. הרשימה שלהלן משקפת אותו (עמוד = מספר גלובלי):

| # | כותרת נושא מודפסת | עמודים | מזהי עמוד פנימיים | תוכן |
|---|---|---|---|---|
| 1 | **שאלות מתוך תוכנית הלימודים** | 1–4 | `C-P1`–`C-P4` | 8 בלוקי `bbb`, ראשונים, מילה במילה (8.4) |
| 2 | **הגדרות ושמונה הזוויות** | 5 | `U1-P5` | הגדרות ישרים/קטעים מקבילים (3.5), שמונה הזוויות, זוויות חד-צדדיות (180°), בדיקת טענות |
| 3 | **זוויות מתאימות ומתחלפות** | 6–7 | `U1-P1`, `U1-P2` | זיהוי מיקומי לפני חישוב (סימון, התאמה, מיון), השלמת המשפט הישיר במילה אחת |
| 4 | **המשפטים הישירים** | 8–9 | `U1-P3`, `U1-P4` | המשפטים הישירים ותנאי המקבילות, דוגמאות־נגד לישרים לא מקבילים, תיקון ניסוח חסר |
| 5 | **חישובי זוויות** | 10–12 | `U2-P1`–`U2-P3` | חישוב מספרי, זיהוי → משפט → חישוב |
| 6 | **חישוב זוויות עם נעלם** | 13–14 | `U2-P4`, `U2-P5` | זיהוי → משפט → משוואה → x → הצבה → זווית (7) |
| 7 | **יישומים** | 15 | `U2-P6` | העברה: הקשר מוחשי ושני חותכים, חישוב מספרי ונימוק |
| 8 | **נימוק והוכחה** | 16–18 | `U3-P1`–`U3-P3` | בחירת המשפט הנכון, בדיקת טענות, הוכחה |
| 9 | **המשפטים ההפוכים** | 19–21 | `U4-P1`–`U4-P3` | המשפטים ההפוכים (3.2) בפיגום מדורג, כולל הדוגמה המודרכת 112° |

סדר הקושי הפדגוגי (5.3) והמניעה מחזרתיות (5.5) חלים על נושאים 2–9 (הנושאים הדידקטיים) לפי סדר הלמידה שלהם, גם כאשר בלוק תוכנית הלימודים ממוקם פיזית ראשון. בלוק תוכנית הלימודים מוחרג ממנגנון המקוריות, ההדרגתיות והמספור-לפני-אלגברה. מיפוי הנושאים למזהי המשימה הפנימיים (`questions-unit*.ts`) עובר דרך `booklet-pages.json` ו־`question-plan.json`; החלוקה לקבצי `unit*` היא ארטיפקט מימוש בלבד.

## נושאים 2–4 — מושגים, זיהוי והמשפטים הישירים (מקור: `questions-unit1.ts`)
מטרה: לבנות הבנה מושגית וזיהוי חזותי לפני חישוב.

הנושאים מתקדמים מן הקל אל המורכב:
- זיהוי שני ישרים וישר חותך.
- זיהוי זוג זוויות מתאימות.
- זיהוי זוג זוויות מתחלפות.
- סימון הזווית המתאימה או המתחלפת לזווית נתונה.
- מיון זוגות: מתאימות / מתחלפות / אחר.
- תצורות אופקיות, משופעות ומסובבות.
- השלמת המשפטים הישירים.
- הבחנה בין עצם הסיווג “מתאימות/מתחלפות” לבין הסקת שוויון.
- זיהוי ותיקון הניסוח החסר “זוויות מתחלפות שוות” או “זוויות מתאימות שוות” ללא תנאי המקבילות.
- נכון/לא נכון, התאמה, טבלה קצרה, בחירה מרובה ומשימות זיהוי נוספות המקובלות בחומרי המקור.

אין אלגברה ואין משפטים הפוכים בנושאים אלה.

קושי: D1 → D2.

## נושאים 5–7 — חישובי זוויות, אלגברה ויישומים (מקור: `questions-unit2.ts`)
מטרה: לעבור מחישוב מספרי ישיר לחישוב משולב ולאחר מכן לאלגברה, בלי לדלג על שלבי הביניים.

סדר פנימי מחייב:
1. חישוב זווית חסרה במספרים באמצעות זוויות מתאימות.
2. חישוב זווית חסרה במספרים באמצעות זוויות מתחלפות.
3. בחירת המשפט המתאים לפני החישוב.
4. חישוב דו־שלבי במספרים.
5. שילוב קודקודיות / צמודות / זווית שטוחה ככלי עזר.
6. טבלאות מספריות ושליפת מידע מהן.
7. תצורות מסובבות ומורכבות יותר.
8. רק לאחר שליטה מספרית — משוואה פשוטה עם `x`.
9. ביטויים אלגבריים בשתי זוויות.
10. מציאת `x` ולאחר מכן חישוב הזווית המבוקשת.

בכל משימה אלגברית שבה נבנית משוואה משוויון זוויות, חייב להיות מקום לכתיבת המשפט שמצדיק את השוויון.

קושי: D1 → D4.

## נושא 8 — נימוק והוכחה: בחירת משפט, בדיקת טענות והוכחה (מקור: `questions-unit3.ts`)
מטרה: מעבר מחישוב לבניית טיעון מתמטי מסודר באמצעות המשפטים הישירים וידע העזר שנלמד.

הנושא מתקדם בהדרגה:
- בחירת המשפט המתאים לנימוק נתון.
- השלמת נימוק חסר בפתרון.
- התאמת טענה לנימוק.
- קביעה ונימוק.
- זיהוי טעות בהוכחה או בנימוק.
- סידור שלבי הוכחה קצרים.
- השלמת שורת הוכחה חסרה.
- כתיבת הוכחה קצרה מלאה ברמת חטיבת ביניים.
- שילוב מתאימות/מתחלפות עם קודקודיות, צמודות או זווית שטוחה כאשר הדבר נדרש.
- מידע עודף או נתון שאינו נחוץ, רק בשלבים מתקדמים של הנושא.

הנושא אינו עוסק עדיין בהסקת מקבילות באמצעות המשפטים ההפוכים; נושא זה נשמר לנושא 9 (המשפטים ההפוכים).

קושי: D2 → D5.

## נושא 9 — המשפטים ההפוכים (מקור: `questions-unit4.ts`)
זהו הנושא המתקדם ביותר בחוברת והוא מופיע רק לאחר שליטה בנושאים הדידקטיים הקודמים.

חובה לכלול:
- השלמת המשפטים ההפוכים למתאימות ולמתחלפות, והמשפט ההפוך לחד-צדדיות כטענה נכונה המוצבת מול זוויות צמודות (3.2, 4–6).
- הבחנה בין משפט ישיר למשפט הפוך.
- זיהוי מתי שוויון של זוויות מתאימות מספיק לקביעת מקבילות.
- זיהוי מתי שוויון של זוויות מתחלפות מספיק לקביעת מקבילות.
- נתון שוויון זוויות → קביעה אילו ישרים מקבילים.
- בדיקת מספיקות נתונים.
- נכון/לא נכון על המשפטים ההפוכים.
- תחילה שאלות מספריות בלבד.
- לאחר מכן מציאת ערך `x` שיבטיח מקבילות.
- בחירת המשפט ההפוך המתאים כנימוק.
- הוכחה קצרה לקביעת מקבילות.
- משימות המשלבות משפט ישיר והפוך רק בשלב המתקדם ביותר.

קושי: D3 → D5.

## נושא 1 — שאלות מתוך תוכנית הלימודים (מקור: `bbb`, ראשון בחוברת)
מטרה: להציג בתוך החוברת שאלות מקור מתוך מאגר תוכנית הלימודים הקיים בריפו `yanivmizrachiy/bbb`, ללא שינוי בתוכן.

כלל נאמנות מחייב:
- מקור: `yanivmizrachiy/bbb` בלבד.
- השאלות נבחרות רק אם הן קשורות מהותית לזוויות בין ישרים מקבילים או להוכחת מקבילות באמצעות קשרי זוויות.
- ניסוח, מספרים, אותיות, סדר נתונים, סעיפים, תשובות לבחירה ותוכן השרטוט נשמרים כפי שהם במקור.
- מותר לבצע רק התאמה טכנית הכרחית לצורך render/print, ורק אם היא אינה משנה תוכן, משמעות, סדר או מראה מתמטי.
- אין להחיל על שאלות אלה מנגנון מקוריות או fingerprint שמטרתו לשנות אותן.
- אין “לשפר” ניסוח גם אם הוא שונה מסגנון הנושאים הדידקטיים.
- מקור השאלה חייב להיות ניתן למעקב באמצעות `sources/curriculum/bbb-parallel-lines.manifest.json`.

נכון לעדכון זה אותרו **8 בלוקי שאלות**:
- 5 שאלות במקטע `ישרים מקבילים — זוויות בין מקבילים` מתוך `geometry8/topics/t02_angles.py`.
- 2 שאלות נוספות במקטע `סכום הזוויות במשולש` באותו קובץ, שבהן ישרים מקבילים הם חלק מהותי מפתרון השאלה.
- שאלה אחת במקטע `שימושים בחפיפת משולשים` מתוך `geometry8/topics/t03_congruence.py`, שבה מוכיחים מקבילות בעזרת זוויות מתחלפות.

בלוק תוכנית הלימודים הוא **בלוק מקור**, ולכן אינו נדרש לעמוד בסולם הקושי של הנושאים הדידקטיים ואינו חלק ממנגנון אי־החזרתיות של השאלות המקוריות.

---

# 5. מודל קושי והתקדמות

## 5.1 רמות D1–D5
- **D1:** זיהוי, סימון, מיון, השלמת מילה, בחירה פשוטה.
- **D2:** יישום חד־שלבי, חישוב ישיר, נכון/לא נכון בסיסי.
- **D3:** שני שלבי הסקה, טבלה, התחלת אלגברה, נימוק קצר.
- **D4:** בחירת דרך, אלגברה, סינון מידע, בניית הוכחה ומשפטים הפוכים בסיסיים.
- **D5:** הוכחה מלאה קצרה, תיקון טעות, שילוב משפטים ותכנון פתרון.

## 5.2 ממדי קושי
לכל משימה מקורית מוערכים בנפרד:
- `conceptualDemand`
- `reasoningDepth`
- `algebraicDemand`
- `visualDemand`
- `languageDemand`
- `decisionDemand`
- `transferDemand`

אין להגדיר שאלה “קשה” רק משום שהמספרים גדולים, הטקסט ארוך או השרטוט מסובב.

## 5.3 סדר התקדמות מחייב
**זיהוי → סיווג → התאמה → ניסוח משפט → תנאי מקבילות → חישוב מספרי חד־שלבי → חישוב מספרי דו־שלבי → טבלה/ייצוג → אלגברה → נימוק → הוכחה → משפטים הפוכים.**

סדר זה מחייב את הנושאים הדידקטיים (2–9). בלוק תוכנית הלימודים הוא בלוק מקור נפרד.

## 5.4 כלל מספרים לפני אלגברה
`x` או ביטויים אלגבריים אינם מופיעים לפני תרגול מספרי של אותה מיומנות בנושאים הדידקטיים.

## 5.5 התקדמות ללא חזרתיות
כל משימה מקורית חייבת להוסיף ערך חדש.

חובה:
- להוסיף לפחות ממד משמעותי אחד: מיומנות, עצמאות, עומק, ייצוג, response mode, טופולוגיית שרטוט, בחירת משפט, סינון מידע או transfer.
- להתקדם בתוך אותה מיומנות: מודרך → עצמאי → תצורה חדשה → קשר נוסף → נימוק/העברה.

אסור:
- שתי שאלות סמוכות עם אותו נתיב פתרון כאשר ההבדל היחיד הוא מספרים, אותיות או סיבוב קל.
- שני סעיפים רצופים הדורשים בדיוק אותה פעולה באותה רמה.
- ליצור שאלה רק כדי למלא שטח בעמוד.

שערים: `duplicates`, `forward-progress`, `local-variety` — חלים על הנושאים הדידקטיים בלבד.

---

# 6. חוזה בניית משימות וגיוון הוראתי

בנושאים הדידקטיים יש לשלב באופן מושכל:
- זיהוי וסימון.
- התאמה.
- השלמת משפט.
- השלמת טבלה ושליפה ממנה.
- חישוב זווית חסרה.
- בחירה מרובה.
- נכון/לא נכון.
- נכון/לא נכון + נימוק.
- קביעה ונימוק.
- בחירת משפט.
- חישוב משתנה.
- ניתוח טענת תלמיד.
- מידע מספיק/לא מספיק.
- איתור טעות.
- הוכחה קצרה.
- משימת העברה בתצורה לא שגרתית.

גיוון נועד לשפר למידה והערכה; אין להשתמש בפורמט חריג שאינו מקובל בדפי עבודה רק לשם “יצירתיות”.

כל שאלה בנושאים הדידקטיים נושאת פרופיל דידקטי פנימי (סוג הפעילות נגזר מן ה־`format` שב־`question-plan.json` דרך `src/content/task-kinds.ts`). **כלל ברזל — אין דמו:** תווית סוג הפעילות **אינה מודפסת בדף התלמיד** בשום סוג משימה — ההוראה עצמה אומרת מה לעשות. סוג המשימה משמש את מדריך המורה (`data-task-kind`) ואת מנגנוני האיכות בלבד, ולעולם אינו מוצג לתלמיד. ממילא אין חשיפה של סוג הזוויות כאשר זיהויו הוא המיומנות הנבדקת.

מסיחים בשאלות אמריקאיות חייבים לנבוע מטעויות תלמיד סבירות: בלבול מתאימות/מתחלפות, שכחת תנאי מקבילות, בלבול קודקודיות/צמודות, בחירת חותך שגוי, או שימוש במשפט ישיר במקום הפוך.

## 6.1 פרופיל חובה לכל משימה מקורית
כל משימה מקורית בנושאים הדידקטיים כוללת לפחות (השדה `unit` הוא provenance פנימי בלבד):
`id`, `unit`, `page`, `difficulty`, `conceptualDemand`, `reasoningDepth`, `algebraicDemand`, `visualDemand`, `languageDemand`, `decisionDemand`, `transferDemand`, `learningObjective`, `assessmentPurpose`, `prerequisites`, `theoremIds`, `format`, `responseMode`, `reasoningSteps`, `misconceptionTarget`, `evidenceOfLearning`, `wordingArchetype`, `instructionVerb`, `sourceArchetypeRefs`, `numericBeforeAlgebra`, `fingerprint`.

בלוק תוכנית הלימודים משתמש ב־source metadata במקום בפרופיל יצירה.

---

# 7. אלגברה — זרם מלא מגאומטריה לזווית

בכל משימה אלגברית מקורית, זרם הפתרון המחייב הוא: **זיהוי זוג הזוויות → כתיבת המשפט המצדיק את הקשר → בניית המשוואה → פתרון `x` → הצבה חזרה → הזווית הסופית במעלות עם סימן `°`.** מציאת `x` אינה סוף הפתרון; התוצר הנדרש הוא גודל הזווית המבוקשת.

- חייב מקום כתיבה צמוד למשוואה, בצד שמאל או בעמודת נימוק סמוכה, עבור **המשפט שמצדיק את הקשר**. מבחינה חזותית: „המשוואה | המשפט המתאים”, ולאחריו פתרון `x` והצבה.
- התלמיד נדרש להבין מדוע ביטוי אחד שווה/משלים לאחר, ולא רק לבצע מניפולציה אלגברית.
- אם מטרת השאלה היא שליפת המשפט, אין להדפיס את הנימוק מראש.
- אזור הנימוק רחב מספיק לכתיבת משפט קצר ביד.
- תשובה מלאה במפתח כוללת את הנימוק הגאומטרי, פתרון המשוואה **והזווית הסופית במעלות**.
- **אין סוגריים כלל בביטויי הזוויות והמשוואות בדף התלמיד.** ביטוי זווית נכתב `3x + 17°` (לא `(3x + 17)°`), וסכום ביטויים נכתב `2x + 35 + 5x − 19 = 180` (החיבור אסוציאטיבי, אין שינוי בחשבון). הסרת הסוגריים אינה משנה את משמעות ה־°.
- כל תשובת זווית נושאת סימן `°` (למשל `40°`, לא `40`).
- **שאלות עתירות משוואות נכתבות בגופן מעט קטן יותר** (`--text-body-dense`), כי האלגברה צפופה והשרטוט נושא את העומס.
- מסלול העבודה קורא **„המשוואה | המשפט המתאים”**: תא המשוואה בצד ימין (תחילת השורה ב־RTL), תא המשפט בשמאל. תוויות המסלול יושבות על רקע לבן מלא ואינן על המשבצות.

שער: `equation-justification` (מורחב לזרם המלא).

---

# 8. חוזה ניסוח ותוכן המיועד לתלמיד

## 8.1 שפה בנושאים הדידקטיים
השפה לתלמיד קצרה, ישירה, פורמלית וטבעית, בסגנון דף עבודה או מבחן ישראלי.

פעלי הוראה מועדפים: `חשבו`, `מצאו`, `קבעו`, `נמקו`, `השלימו`, `סמנו`, `בחרו`, `רשמו`, `כתבו`, `התאימו`.

ארכיטיפים מקובלים:
- “כתבו בכל סעיף האם …”
- “השלימו את המשפט.”
- “בחרו את התשובה הנכונה.”
- “קבעו אם הטענה נכונה או לא נכונה. נמקו.”
- “סמנו בשרטוט זוג זוויות …”
- “חשבו את ערכו של …”
- “השלימו את הטבלה.”
- “על סמך הנתונים, קבעו אם הישרים מקבילים. נמקו.”

## 8.2 תוכן שמותר להופיע בדף תלמיד
- כותרת הפרויקט (שם החוברת) ומספר העמוד (מספר-בלבד בעיגול).
- הוראות פתרון אמיתיות.
- שאלות, נתונים, טבלאות ושרטוטים.
- משפט מתמטי שהתלמיד צריך ללמוד, להשלים או להשתמש בו.
- אזורי תשובה/חישוב/נימוק.
- footer הקנוני.

## 8.3 תוכן שאסור להופיע בדף תלמיד
- הוראות למפתח או לסוכן AI.
- מטרות QA או הסברים על שיקולי עיצוב.
- שמות פנימיים כגון D1, V2, fingerprint, validator, component וכדומה.
- placeholder, demo, sample, “כאן תופיע שאלה”, “בקרוב” או סטטוס פיתוח.
- ניסוחים שנלקחו מהסברי המשתמש אך אינם טבעיים לתלמיד.
- ניסוחי AI שיחתיים כגון “בואו נגלה”, “מה אתם חושבים?”, “אתגר מגניב”.
- סיפור מסגרת מיותר או תרגום מילולי מאנגלית.

## 8.4 נורמליזציה בבלוק תוכנית הלימודים
בבלוקי תוכנית הלימודים הטקסט המקורי מ־`bbb` נשמר כפי שהוא ואין להפעיל עליו rewrite אוטומטי. **החריג היחיד:** הביטוי „בין מקבילים” מוצג לתלמיד כ„בין ישרים מקבילים”. זו נורמליזציית תצוגה מוגבלת לביטוי מדויק זה בלבד; טקסט המקור נשמר verbatim ב־provenance ואינו משתנה.

שער: `student-facing-copy` + `wording-style` + `parallel-wording` לנושאים הדידקטיים; `curriculum-source-integrity` + `parallel-wording` לבלוק תוכנית הלימודים.

## 8.5 קומיקס בשאלת בדיקת טענה
בשאלה שבה שני תלמידים טוענים טענות מנוגדות (בדיקת טענה), הטענות מוצגות כ**קומיקס של שתי דמויות בנים** (`src/components/ClaimComic.tsx`): לכל דובר אווטאר בסגנון קו נקי (מילוי נייר, קו דיו, חולצה בצבע הדגש) ובועת דיבור עם טענתו ושמו. הדמויות ידידותיות אך מאופקות ובטוחות לשחור־לבן ול־forced-colors. הטענות המלאות (כולל המשפט הקנוני) חיות בקומיקס; ההוראה מציגה את התרחיש ושואלת מי צודק. הרכיב אינו יוצר גלישת רוחב (בועת הדיבור אינה חורגת מן העמודה).

---

# 9. עברית, Bidi וכתיב מתמטי

- RTL מלא וטבעי.
- התאמה נכונה של זכר/נקבה ויחיד/רבים.
- פיסוק עברי תקין.
- אין רווחים כפולים או רווח לפני סימני פיסוק.
- סימן מעלה נצמד לערך: `64°`.
- מינוח אחיד: “זוויות מתאימות”, “זוויות מתחלפות”, “זוויות חד-צדדיות”, “ישר חותך”, “ישרים מקבילים”.
- **ניסוח הקשר תמיד „בין ישרים מקבילים”, לעולם לא „בין מקבילים”** — בכל הנושאים, כולל תצוגת בלוקי תוכנית הלימודים. ניסוח המקור נשמר ב־provenance בלבד. שער: `parallel-wording`.
- **אין להוסיף סוגריים דקורטיביים** סביב ביטוי שלם לשם עיצוב; סוגריים רק כשהם נדרשים מתמטית.
- מתמטיקה, אותיות לטיניות ומספרים בתוך עברית מבודדים LTR במנגנון תקני.
- מנוע מתמטי: **MathJax 4.x**; SVG output מועדף כאשר הוא משפר הדפסה.
- ביטוי מתמטי שלם — כולל אופרטורים, סימן שוויון וסימני יחס (למשל `∠A + ∠C = 180°` או `2x + 35 + 5x − 19 = 180`) — מוצג כאי LTR **אחד**. אסור לפצל ביטוי לכמה איים מבודדים: בפסקה RTL סדר האיים מתהפך והתלמיד קורא מתמטיקה שגויה (למשל „∠C = 180° + ∠A”). מילים עבריות וסימני פיסוק עבריים אינם נכנסים לעולם לתוך אי מתמטי. שער: `math-island-integrity`.
- סימון אחיד: `∠ABC`, `AB ∥ CD`, `x`, `α`, `β`, `γ`, `180°`.
- בבלוק תוכנית הלימודים נאמנות למקור גוברת על normalization לשוני, למעט התיקון הטכני „בין מקבילים” ← „בין ישרים מקבילים” ותיקון טכני שאינו משנה את התוכן המוצג.

---

# 10. שרטוטים ומורכבות חזותית

כל שרטוט גאומטרי מקורי שניתן ליצור וקטורית חייב להיווצר כ־**SVG**.

## 10.1 סולם Visual Demand
- **V1:** ישרים אופקיים/כמעט אופקיים, חותך ברור, מעט תוויות.
- **V2:** זוג ישרים משופע, חותך בזווית אחרת.
- **V3:** תצורה מסובבת/כמעט אנכית; אי־אפשר להסתמך על “למעלה/למטה”.
- **V4:** שני חותכים, יותר תוויות, נתון עודף או מסלול חלופי.

## 10.2 כללי גיוון שרטוט
- שיפוע הישרים וכיוון החותך משתנים באופן מכוון לאורך החוברת.
- אין להשתמש שוב ושוב באותה תבנית שרטוט עם מספרים אחרים.
- העלייה ב־visualDemand הדרגתית.
- אין להעלות בו־זמנית קושי חזותי ומתמטי בצורה חדה ללא מטרה פדגוגית.

## 10.3 יכולות מנוע השרטוט
- **קשתות סמוכות:** שתי קשתות בסגנונות שונים בגזרות הסמוכות לאותה קרן באותו מפגש לעולם אינן נפגשות לקשת אחת: הסגנון המאוחר (רגילה → מקווקוות → כפולה) מתרחק החוצה בפער קבוע (הטוקן `arc.neighbourStepMm` ב־`src/styles/tokens.ts`).
- **דיוק מד־זווית:** כל זווית שמספרה מודפס (מעלות או ביטוי שערכו ידוע מן המפתח) משורטטת בגודלה האמיתי בסטייה של עד 1°; הבדיקה `independent-math-verification` אוכפת זאת. גיוון השרטוט (10.2) לעולם אינו גובר על דיוק זה.
Point, Vector, Line, Segment, ישרים מקבילים, חותך אחד/יותר, נקודות חיתוך, קשתות זווית, סימני מקבילות, תוויות, מעלות/משתנים, collision detection, minimum spacing, auto-fit, auto-scale ו־viewBox.

אסור: תווית על קו, קשת שחוצה טקסט, או שרטוט שמטעה לגבי זווית חדה/קהה או יחס בסיסי.

## 10.4 חריג שרטוטי בלוק תוכנית הלימודים
שרטוטי המקור נשמרים נאמנה. אם נדרשת המרה טכנית לפורמט render אחר, התוצאה חייבת להיות שקולה חזותית ומתמטית למקור; אסור לשנות מיקום נקודות, סימונים, נתונים או יחס גאומטרי.

## 10.5 מנוע מיקום תוויות ובטיחות וקטורית
בשרטוטים המקוריים של הנושאים הדידקטיים:
- מיקום תוויות זווית מחושב באופן collision-aware ולא באמצעות היסט קבוע בלבד.
- לכל תווית מחושב bounding box משוער/נמדד, ונבחר מיקום מתוך מספר רדיוסים וזוויות מועמדות.
- תוויות אינן רשאיות לחפוף זו לזו, לצאת מגבולות ה-viewBox הבטוחים או להסתיר נקודת חיתוך.
- נשמר safe inset קבוע בתוך ה-viewBox וכן minimum spacing בין תוויות.
- auto-fit ו-auto-scale חייבים לשמור על יחס גאומטרי, עובי קו וקריאות הטקסט.
- קווים, קשתות, סימוני מקבילות ותוויות נשארים וקטוריים; אין rasterization של שרטוט מקורי.
- כל SVG תלמיד נושא role נגיש, תיאור סמנטי ותווית משמעותית. התיאור מציין רק את מה שמשורטט בפועל: זוג ישרים נקרא „מקבילים” רק כאשר מסומנים סימוני מקביליות.
- שום תווית (של ישר, של חותך או של זווית) אינה יושבת על קו משורטט; מיקום התוויות מתחשב גם בקווים ולא רק בתוויות אחרות. שער: `label-line-clearance`.
- לכל זווית מסומנת יש תפקיד דידקטי — נתון / זווית יעד / זווית מסומנת לזיהוי / זווית עזר — והעיצוב (צבע וצורת קשת) נקבע מטבלה מרכזית אחת (`src/geometry/angle-roles.ts`), כך שאותו תפקיד נראה זהה בכל החוברת וגם בשחור־לבן. עיצוב ידני מותר רק כאשר נוסח המשימה עצמו מתייחס לצורת הקשת.
- בשרטוט שלושה ישרים, תווית זווית נשארת בתוך הסקטור של הזווית שהיא מתארת ואינה נדחפת אל מעבר לקו.
- קשר מתמטי אינו מקודד בצבע בלבד: סוג קו, dash, קשת כפולה/יחידה, badge או סימון צורני משמשים redundancy.
- דוגמת־נגד שבה הישרים **אינם** מקבילים משורטטת עם ישרים שאינם מקבילים בפועל (הטיה הנראית לעין בהדפסה) וללא סימוני מקביליות. כאשר המקבילות פשוט **אינה נתונה**, מותר שהישרים ייראו מקבילים — אך ללא סימוני מקביליות, והמסקנה הנדרשת היא „לא ניתן לקבוע”. ציור שנראה מקביל אינו הוכחה.
- סקטור הזווית, הקשת והתווית מחושבים לפי כיוון הישר שבאותה נקודת חיתוך, כך שהסימון יושב בדיוק על הזווית הנכונה גם כאשר הישרים אינם מקבילים.

שער: `geometry-collision` + `svg-geometry`.

---

# 11. חוזה עיצוב והדפסה

## 11.1 A4 וניצול שטח — כלל קשיח
- בכרטיסי תוכנית הלימודים, השטח שנותר מתחת לתוכן המקור ממשיך את שורות הכתיבה המנוקדות של המקור (אותו מרווח וצבע) — שינוי מסגרת בלבד, ה־DOM והטקסט של המקור אינם משתנים.
- שרשרת הסקה מודרכת שמקבלת גובה עודף מפזרת את שלביה באופן שווה, ואינה משאירה רצועה ריקה.
- כל עמוד הוא A4 מדויק: **210×297 מ״מ**.
- כל שטח העבודה הזמין מנוצל באופן מאוזן.
- אסור להשאיר שליש עליון, אמצעי או תחתון ריק ללא הצדקה פדגוגית.
- רווח לבן מותר רק אם הוא משרת קריאות, הפרדה, אזור תשובה או היררכיה.
- אם נותר חלל גדול: reflow, הרחבת אזור תשובה, הגדלת שרטוט באופן סביר, שינוי פריסה או העברת משימה מתאימה.
- אין למלא חלל באמצעות מלל דמו או קישוט חסר ערך.
- אין לפתור עומס באמצעות הקטנת טקסט מתחת לסף קריאות.
- תוכן העמוד לעולם אינו חורג מאזור התוכן (`usedSpanRatio ≤ 1`): עמוד שתוכנו גולש לתחום ה־footer או נחתך בגבול 297 מ״מ אינו תקין ואינו רשאי להיכנס ל־baseline. שער: `layout-health`.

שער: `page-utilization`.

## 11.2 אחידות ספר לימוד
- מערכת טיפוגרפית אחת.
- design tokens משותפים לכותרות, גוף, נוסחאות, תוויות, קווים, קשתות, צבעים, תיבות וטבלאות.
- כל השרטוטים המקוריים נראים כאילו הופקו על ידי אותו מאייר מתמטי ובאותו ספר.
- אין פונט או styling אד־הוק ללא הצדקה.
- בבלוק תוכנית הלימודים אין לשנות תוכן מקור כדי להשיג אחידות; ניתן לעטוף את שאלת המקור במסגרת העמוד האחידה בלבד.

## 11.3 צבע
החוברת יכולה להיות צבעונית, אך צבע משמש **למטרה דידקטית בלבד**: הדגשת זוג זוויות, הבחנה בין חותכים, נתון מול נדרש, או קידוד עקבי שאינו מסגיר פתרון.

אסור להעביר מידע חיוני באמצעות צבע בלבד.

## 11.4 שחור־לבן
כל עמוד חייב להישאר ברור ושימושי ב־grayscale. מידע חיוני מובחן גם בצורה, סוג קו, עובי, תווית או מיקום.

שער: `grayscale`.

## 11.5 מספור שאלות וסעיפים
- **כל שאלה ראשית נפתחת ב־● (נקודה שחורה מלאה גדולה); כל סעיף ב־• (נקודה מלאה קטנה יותר).** צבע שחור (--ink), במיקום, גודל ויישור קבועים מ־design tokens. רק העמודים ממוספרים.
- ● / • הם סימני פתיחה של שאלה וסעיף — לא מספור ולא תווית. אין מספרי שאלות ואין אותיות סעיף.
- הסימנים אחידים בכל העמודים ונקראים היטב גם בשחור־לבן.
- בבלוק תוכנית הלימודים סימן ה־● בראש כל שאלה הוא chrome חיצוני (אינו טקסט מקור).
- **אין תוויות סוג משימה** ואין כותרת נושא בדף התלמיד (כלל הברזל „אין דמו”, ראו 4.2 ו־6).
- **אין קו מפריד בין שאלות** — ההפרדה היא רווח שקט בלבד.
- **הערות הסבר (מוסכמות/תזכורות, כגון „חצים זהים על שני ישרים מסמנים שהישרים מקבילים”) מוצגות בעיצוב הערה נפרד** (`hint-note`): מסגרת מרומזת עם פס דגש בצד, סימן ⓘ וקול הסברי (נטוי), לעולם לא כחלק מן ההוראה.
- **משבצות (דף חשבון) הן אך ורק היכן שמחשבים** — עבודה, אלגברה, שתי דרכים, ומסלול „המשוואה | המשפט המתאים”. **אזורי נימוק/הסבר בפרוזה** („נמקו/הסבירו”, פסק דין נכון/לא נכון) **וטבלת ההוכחה הריקה (mode `proof`) מנוקדים בפסיעת הכתיבה — לא משבצות**: שם המשבצות הן רעש ולא עזר. תוויות המסלול על רקע לבן מלא.

## 11.6 מספור עמודים רציף
- מספור עמודים רציף יחיד 1..N לכל אורך החוברת, ללא איפוס ובלי „עמוד 1” חוזר.
- מספר העמוד מוצג כמספר-בלבד בתוך עיגול בפינה השמאלית-עליונה.
- preview, PDF ומפתח התשובות משתמשים באותה שיטת מספור רציפה.

שער: `circle-page-number` + `continuous-numbering`.

## 11.7 מבנה עמוד ושאלה
- אזור תשובה מותאם לסוג המשימה.
- אין clipping, overflow או פיצול שפוגע בהבנה.
- העימוד נראה כמו ספר לימוד ולא כמו Word/dashboard/web app.
- השאלות יושבות ישירות על רקע לבן של הדף: אין קופסאות, רקעים מנומרים, צללים או פסי צבע סביב שאלה או סביב שרטוט. ההפרדה בין שאלות נעשית ברווח שקט ובהיררכיה טיפוגרפית בלבד.

## 11.8 Footer קנוני
בכל דף תלמיד מופיע footer אחיד בשתי שורות:

**יניב רז - מדריך מחוזי חט"ב בעיר ירושלים**

**הדרכה במחוז ירושלים והעיר ירושלים - מנח"י, בהובלת איילת קריספין**

מעל שתי השורות מופיע **קו כחול־כהה דק לכל רוחב הדף** (יוקרתי, ~0.2 מ״מ). מלל ה־footer מודגש. בצד ימין של ה־footer מופיע **הלוגו העגול של המחוז** (הסמל „יחד מתמטיקה · מחוז ירושלים”, `src/assets/district-logo.png`, נשאב מ־`yanivmizrachiy/jerusalem`), צמוד וממוזער. ה־footer עדין ואינו מתחרה בתוכן.

## 11.9 הפקת PDF כפולה ו-cross-check
- **Vivliostyle CLI** הוא מנוע ה-typesetting הייעודי ל-Paged Media ומפיק את PDF התלמיד הקנוני מתוך build ה-HTML הסטטי.
- **Chromium דרך Playwright** מפיק PDF עצמאי נוסף מאותו build ומשמש renderer שני לבקרת רגרסיות.
- גרסת Vivliostyle CLI ננעלת לגרסה יציבה מפורשת; בעת עדכון יש לאמת מחדש את כל החוברת.
- שני ה-PDF חייבים להכיל בדיוק את מספר העמודים שמגדיר `booklet-pages.json` (כיום 21), כולם בגודל A4 portrait בטולרנס זעיר.
- cross-check חייב לוודא קיום, מספר עמודים, מידות עמודים וגודל קובץ לא טריוויאלי בשני המנועים.
- כשל באחד משני המנועים או ב-cross-check מפיל CI; אין "fallback שקט".
- Chromium נשאר מנוע ה-visual QA: צילומי color, grayscale ו-forced-colors לכל העמודים.
- צבעי הדפסה משתמשים ב-`print-color-adjust: exact` כאשר הדבר נתמך, אך משמעות מתמטית נשארת תקינה גם ללא צבע.

שער: `pdf-dual-engine` + `pdf-crosscheck` + `forced-colors`.

## 11.10 רגרסיה חזותית קנונית
- `qa/visual-baseline.json` הוא baseline מאושר של כל דפי התלמיד (מספרם נגזר מ־`booklet-pages.json`).
- לאחר הפקת Chromium נמדדים מבנה העמוד, מספר שאלות/סעיפים/שרטוטים, MathJax, ניצול שטח, gaps ומידות A4.
- שדות מבניים מושווים באופן קשיח; מדדי layout מושווים בטולרנסים קטנים ומפורשים כדי למנוע כשלי רעש של renderer.
- שינוי שחורג מן ה־baseline מפיל CI. עדכון baseline מותר רק לאחר בדיקה מכוונת של השינוי החזותי.
- עדכון ה־baseline נעשה אך ורק באמצעות `npm run qa:rebaseline -- --accept`: הכלי מציג כל הבדל לבדיקה, וללא `--accept` אינו כותב דבר. הכלי מסרב לקדם render לא תקין (חריגה מאזור התוכן, התנגשויות גאומטריות, חפיפה עם header/footer, MathJax חלקי). השער והכלי חולקים חוזה אחד: `scripts/lib/visual-baseline-contract.mjs`.

שער: `visual-baseline`.

## 11.11 מדריך מורה מבודד
- מופק PDF מורה נפרד מתוך `src/content/answer-key.ts` בלבד; אין מקור תשובות כפול.
- build המורה נוצר ב־`dist-teacher/` ואינו נכלל ב־`dist/` שמיועד ל־GitHub Pages של התלמידים.
- מפתח המורה מכסה את כל 62 המשימות המקוריות של הנושאים הדידקטיים.
- בבלוק תוכנית הלימודים אין להמציא תשובות: מוצגת מדיניות המקור עד להימצאות פתרונות מפורשים ומאומתים.
- PDF המורה חייב לעבור בדיקת ספירת תשובות, MathJax, גודל קובץ ו־A4 לכל עמוד.
- המדריך כתוב למורה: שדות התשובה מוצגים בכותרות עבריות (תשובה, התשובה הנכונה, נימוק, הוכחה …), זוויות מחושבות מוצגות עם סימן מעלה (`∠B = 68°`) ומשתנים בלעדיו (`x = 16`), וכל תשובה נושאת את תווית סוג המשימה (גם במשימות הוכחה, שבהן התווית אינה מודפסת בדף התלמיד — ראו 6). אין להציג מזהים פנימיים, מפתחות נתונים באנגלית, שמות קבצים, שמות ריפו או קודי סטטוס. שדה תשובה ללא כותרת מוגדרת הוא שגיאת build.

שער: `teacher-pdf`.

- מדריך המורה מעוצב כמפתח תשובות מודפס שטוח (בלי כרטיסים, אריחים, רקעים או פינות מעוגלות; קווי הפרדה דקים). כל תשובה מזוהה לפי **מספר העמוד ומיקום השאלה בעמוד** (השאלה ה־k בעמוד, כלומר ה־● ה־k) ומילות הפתיחה של השאלה (למשל „עמוד 10 · שאלה 3 בעמוד”). שאלות אינן ממוספרות, ולכן אין מספר שאלה גלובלי. הערות מורה מתארות מיקומי זוויות במילים (מעל/מתחת לישר, מימין/משמאל לחותך) ולא במונחי המנוע.

## 11.11א מערכת העיצוב — מקור אחד לכל ערך

- **טוקנים:** כל custom property מוגדר ב־`src/styles/tokens.css` בלבד (טיפוגרפיה, צבעים, ריווח, מסגרת, מקצב שאלה, שורות כתיבה, משבצות). מידות השרטוט הפיזיות ב־`geometryTokens` (`src/styles/tokens.ts`). `page-tuning.css` מכיל רק presets של צפיפות (`data-density` מ־`src/content/page-layout.ts`) ורק custom properties — שער `layout-tokens`.
- **גופן העברית:** Noto Sans Hebrew ארוז בחבילה (`@fontsource-variable/noto-sans-hebrew`, OFL-1.1, גרסה מוצמדת), כך שכל מכונה ושני מנועי ה־PDF שוברים שורות זהות.
- **גופנים:** כל `@font-face` ב־`src/styles/fonts.css`, נטען מקומית מהבאנדל (ללא CDN). תוויות שרטוט וסימנים בטקסט רץ (● • ○ ∠ ∥ △ ≅ ואותיות יווניות) בגופן New Computer Modern — אותו גופן שבו MathJax מסדר את הנוסחאות; עברית ב־Noto Sans Hebrew. החריג היחיד: הגופן שהמקור של בלוק תוכנית הלימודים מצהיר עליו בתוך התרשימים שלו (נשמר verbatim).
- **שרטוטים בקנה מידה פיזי:** כל שרטוט מצויר במנוע אחד (`src/geometry/engine.ts` + `primitives.tsx`) בגודל שנבחר מהנתונים (`diagram-size.tsx`: compact / full / mark / markLarge / pair / densePair / table / dense). תוויות מודפסות ב־9.5pt (מספור 9pt, לעולם לא פחות מ־8.5pt), קווים 0.40 מ״מ, קשתות 0.35 מ״מ. ה־viewBox מותאם לשרטוט עצמו. תווית היא טקסט עם הילה לבנה (לא "צ'יפ" ממוסגר), בתוך הסקטור של הזווית שלה, ולעולם לא על קו או על קשת. אותיות בכתב נטוי מתמטי כמו בגוף השאלה. נקודות חיתוך מלאות; קווקוו רק לתפקיד עזר.
- **צבע לפי תפקיד בלבד:** נתון / זווית יעד / זווית מסומנת / עזר (`angle-roles.ts`). צבע לעולם אינו מקודד את סוג הקשר (מתאימות/מתחלפות); כל זווית שהשאלה מזכירה בשמה מסומנת בשרטוט באותו שם — שערים `angle-label-placement`, `stem-diagram-labels`.
- **אזורי תשובה לפי פורמט — משבצות רק היכן שנדרש:** `src/content/answer-areas.ts` מגדיר לכל פורמט את אזור התשובה (משבצת תשובה סופית, מסלול „המשפט המתאים”, שורות עבודה, **רשת משבצות (דף חשבון)**, טבלת הוכחה, פסק דין לכל טענה, שתי דרכים). **רשת משבצות מופיעה אך ורק במשימות הדורשות עבודה אמיתית**: גזירה, פתרון רב־שלבי, אלגברה או שרטוט עזר. משימות זיהוי / בחירה / סימון / מילה אחת / תשובה קצרה **אינן מקבלות רשת גדולה** — לכל היותר משבצת תשובה סופית קטנה או שורה בודדת. **גובה אזור העבודה תואם את העבודה הצפויה** ואינו רווח מת. שורות כתיבה מנוקדות בפסיעה 8 מ״מ (preset צפוף 7 מ״מ, לא פחות), רווח שקט בין שאלות, ≥3 מ״מ מהשורה האחרונה. כל שאלה שמבקשת „נמקו / הסבירו / הוכיחו / ציינו / בשתי דרכים” מקבלת לפחות שתי שורות או טבלת הוכחה — שערים `answer-areas` + `answer-grid`.
- **ניצול A4:** בלוקי השאלות גדלים לפי משקל אזור התשובה שלהם; עודף הגובה בעמוד הופך לשורות כתיבה שלמות, ולא לרווח מת בתוך הבלוק. יעד: 0 חריגה מהעמוד, ורווח מת ≤ 8 מ״מ בכל בלוק.
- **כותרת עמוד:** כותרת הנושא היא האלמנט המוביל; שם החוברת הוא running head שקט; מספר העמוד מוצג כמספר-בלבד בתוך עיגול בפינה השמאלית-עליונה (ללא המילה „עמוד” וללא „יחידה”). הטקסטים עצמם קבועים.
- **PDF כפול:** ה־snapshot של Vivliostyle הוא עצמאי — כל גליף MathJax מוטמע בנוסחה שלו (בלי `<use>` למטמון משותף) — שער `vivliostyle-self-contained`.

## 11.12 דטרמיניזם, שלמות ו־Release
- `package-lock.json` tracked ומחייב; CI משתמש ב־`npm ci` וב־npm cache המבוסס על lockfile.
- סביבת Node pinned במפורש ל־Node `22.23.2` ב־CI, Release, `package.json` ו־`.nvmrc`; אין שימוש בגרסת major נעה.
- סביבת Python pinned במפורש ל־Python `3.14.7` באמצעות `actions/setup-python` pinned ל־SHA וב־`.python-version`; אין הסתמכות על Python המתחלף של runner.
- קבצי טקסט נשמרים ב־UTF-8 עם LF באמצעות `.editorconfig` ו־`.gitattributes`, גם ב־Windows; `.npmrc` אוכף `engine-strict` ו־`save-exact`.
- כל dependency ישיר חייב להיות pinned לגרסה מדויקת; lockfile חייב להיות v3, עם registry ו־integrity תקינים.
- CI ו־Release מריצים `npm audit --omit=dev --audit-level=high`; פגיעות production ברמת high/critical מפילות build.
- אין שום אוטומציה מתוזמנת: אסור `schedule`, cron, Dependabot מחזורי או פעולה שבועית/יומית. אוטומציה מותרת רק על push, pull request, tag/Release או workflow ידני.
- כל שימוש ב־GitHub Action חיצוני ב־CI/Release pinned ל־commit SHA מלא; תגי `@vN` אינם מקור אמת להרצה.
- כל build מפיק `SHA256SUMS.txt` לכל קובצי ה־PDF.
- `input-fingerprint.json` מחושב אוטומטית מתוך `git ls-files` ומכיל SHA-256 לכל קובץ tracked בריפו וכן aggregate SHA-256 יחיד; אין רשימת קלטים ידנית שעלולה להתיישן.
- `build-manifest.json` קושר יחד גרסת package, commit/ref/run, גרסת Node, fingerprint הקלטים, SBOM ושלושת קובצי ה־PDF.
- כל build מפיק CycloneDX SBOM ב־`artifacts/sbom.cdx.json` ישירות מ־npm/lockfile, ללא רשימת תלויות ידנית כפולה.
- Release נוצר אוטומטית רק ב־push של tag התואם `v*`, ורק לאחר build ו־QA מלאים מאותו tag.
- שם ה־tag חייב להיות בדיוק `v<version>` של `package.json`; mismatch מפיל Release.
- Release כולל את שלושת ה־PDF, checksums, build manifest, SBOM, input fingerprint, provenance manifest ודוחות ה־QA.
- אין ליצור Release אוטומטי מכל commit ל־main.

שער: `deterministic-install` + `dependency-policy` + `dependency-audit` + `no-scheduled-automation` + `actions-pin` + `sbom` + `input-fingerprint` + `checksums` + `release-contract`.

---


## 11.13 נגישות מרונדרת
- מסמך התלמיד משתמש ב-`lang="he"` ו-`dir="rtl"`, וכל דפי ה־A4 נשארים RTL.
- אין ריבוי landmarks מסוג `main` בתוך חוברת התלמיד; כל דף הוא `article` עצמאי.
- כל SVG גאומטרי נושא `role="img"` ו-`aria-label` משמעותי.
- כל נוסחת MathJax המרונדרת שומרת תיאור ARIA.
- IDs ב־DOM חייבים להיות ייחודיים, ואסור שיהיו רכיבים focusable בתוך `aria-hidden="true"`.
- מדריך המורה חייב להכיל `main` יחיד, `h1` יחיד, RTL/עברית תקינים ונוסחאות נגישות.
- תוצאות בדיקת התלמיד נשמרות ב-`artifacts/accessibility-report.json`; בדיקת המורה נשמרת בתוך `teacher-pdf-report.json`.
- PDF התלמיד שמופק ב־Chromium ו־PDF המורה מופקים עם `tagged: true`.
- QA בינארי מאמת שקיים `/StructTreeRoot` ב־PDFים המתויגים; כתיבת option בלבד אינה מספיקה.

שער: `accessibility` + `tagged-pdf`.

---

## 11.14 רכיב חץ מסקנה משותף (חץ כפול ⇓)

- קיים רכיב יחיד ומשותף לחץ מסקנה **כפול** כלפי מטה (⇓ — שני קווים מקבילים הנפגשים בראש חץ אחד), המשמש בכל מקום שבו נדרשת מסקנה מדורגת (פיגום המשפטים ההפוכים, גזירות מודרכות, „לכן…”, רצף נתון → מסקנת ביניים → מסקנה).
- החץ **זהה בכל מופעיו** (אותו עובי, אורך, מרווח ויישור מ־design tokens), **כלפי מטה תמיד**, **בטוח ל־RTL** (אינו עובר mirroring או היפוך סדר על ידי bidi), ו**PDF-safe** — מתרנדר וקטורית ובאופן זהה ב־Vivliostyle וב־Chromium; אינו תלוי בגליף גופן שעלול להיעדר, אינו שובר line-height, ונגיש.
- אין ליצור חצי מסקנה ad-hoc; כל שימוש עובר דרך הרכיב המשותף. במקומות מתאימים אפשר ליצור רצף אנכי של כמה שלבים.
- **בטבלת הוכחה (טענה | נימוק) חץ מסקנה ⇓ מוביל כל טענה אחרי הראשונה**, כך שעמודת הטענה נקראת כהסקה שלב־אחר־שלב. חריג: בטבלת „סדר את ההוכחה” שורות מעורבבות בכוונה — אין בה חצים.

שער: `conclusion-arrow`.

---


# 12. מקוריות ומניעת כפילויות

בנושאים הדידקטיים לכל משימה יש fingerprint הכולל לפחות: מיומנות, משפט נדרש, טופולוגיית שרטוט, סוג נתון, סוג נדרש, שלבי הסקה, response mode, רמת קושי, משפחת ערכים, אותיות/סמלים, wordingArchetype, instructionVerb, misconceptionTarget, sourceArchetypeRefs ורמת transfer.

שתי שאלות מקוריות נחשבות דומות מדי אם נתיב הפתרון והמבנה כמעט זהים גם כאשר המספרים/אותיות שונים.

בלוק תוכנית הלימודים מוחרג ממנגנון מקוריות: תפקידו לשמר חומר מקור, לא ליצור וריאציה.

---

## 12.1 Provenance וזכויות מקורות
- לכל אחד מ־30 המקורות ב-`sources/manifest.json` נשמר מזהה Google Drive, קטגוריה, אופן שימוש וסטטוס זכויות.
- `rightsStatus: "not-asserted"` פירושו שהפרויקט אינו טוען לבעלות או לרישיון שלא אומתו.
- אסור להסיק רישיון, בעלות או היתר שימוש מתוך שם הקובץ, הכותרת או עצם שמירתו ב־Google Drive.
- `usageMode` מבדיל בין reference חיצוני, reference חזותי, מקור הדרכתי, worksheet reference ו־core reference.
- שינוי או הוספת מקור בלי provenance מלא מפילים CI.

שער: `source-provenance`.

---

# 13. טכנולוגיות מימוש

יש להשתמש בטכנולוגיה החזקה והמתאימה ביותר כאשר היא משפרת דיוק, איכות, אחידות או בדיקות:

- **TypeScript strict** — מודלים, לוגיקת תוכן ו־validators.
- **React + Vite** — preview וקומפוזיציית עמודים.
- **SVG** — שרטוטים וקטוריים.
- **MathJax 4** — מתמטיקה ב־SVG.
- **Vivliostyle + CSS Paged Media** — מנוע PDF קנוני.
- **Chromium דרך Playwright** — מנוע PDF עצמאי נוסף, browser QA וצילומי color / grayscale / forced-colors.
- **Zod** — schema validation של manifests ותוכניות התוכן.
- **Vitest** — unit/integration contracts.
- **Python 3** — rendering טכני של חומר המקור הקנוני בבלוק תוכנית הלימודים בלבד; אין תלות ב־SymPy.
- **Git + GitHub** — versioning.
- **npm lockfile + npm ci** — התקנה דטרמיניסטית ושחזור build.
- **GitHub Actions** — CI, הפקת artifacts, visual baseline, cross-check של מנועי PDF ו־Release מתגים.
- **SHA-256** — אימות שלמות של תוצרי PDF.
- **תצוגה חיה בפיתוח** — Vite HMR עם בקר רענון dev-only (`src/dev/DevRefreshBar.tsx`), הנטען רק מאחורי `import.meta.env.DEV` ולכן אינו נכלל ב־build, ב־PDF או במדריך המורה. הבקר מציג בזמן אמת עמודים שתוכנם חורג מאזור התוכן ואת שלמות ה־MathJax, ומתג „מזהים” המסמן כל שאלה ושרטוט ומציג את רשומת התוכן שמאחוריהם.

---

# 14. אימות מתמטי

כל שאלה חישובית מקורית נפתרת גם על ידי מנגנון אימות שאינו תלוי בקוד התצוגה.

חובה לבדוק:
- עקביות בין שרטוט לנתונים.
- תשובה יחידה כאשר היא מצופה.
- זוויות בטווח תקין.
- אלגברה ללא פתרון שגוי/דו־משמעי.
- התאמה בין המשפט לנתונים.
- אין הסקת שוויון ממתאימות/מתחלפות ללא תנאי המקבילות.
- במשפט הפוך, הנתון אכן מספיק לקביעת מקבילות.
- במשוואה, המשפט הכתוב כנימוק אכן מצדיק את השוויון שנבנה.

בבלוק תוכנית הלימודים נדרש בנוסף `curriculum-source-integrity`: התוכן המוצג חייב להיות זהה סמנטית ומילולית למקור שנרשם ב־manifest.

המנגנון העצמאי ממומש ב־`tests/unit/independent-math-verification.test.ts`: הוא פותר כל משימה חישובית מן הנתונים באופן עצמאי, קורא את ה־SVG המרונדר, ומסווג כל זוג זוויות מסומנות (מתאימות / מתחלפות / קודקודיות / צמודות) מעקרונות ראשונים. כל פגם שנמצא ותוקן נשמר כבדיקת רגרסיה בעלת שם (למשל „regression: CI-11”). כל משימת חישוב המשתמשת במשפט ישיר מציינת את תנאי המקבילות במילים, ולא רק בסימוני השרטוט. שער: `independent-math-verification`.

---

# 15. ארכיטקטורת הריפו

העץ הבא מתאר את המבנה הקנוני בפועל. אין לשמור ב־repo תיקיות build/generated כמו `dist/`, `dist-teacher/`, `artifacts/`, `node_modules/`, `.vivliostyle/` או `src/content/generated/`; הן נוצרות בזמן build ונשארות מחוץ ל־Git.

```text
Makbilim/
├─ SPEC.md
├─ README.md
├─ package.json
├─ package-lock.json
├─ .nvmrc
├─ .python-version
├─ .npmrc
├─ .gitattributes
├─ .editorconfig
├─ tsconfig.json
├─ vite.config.ts
├─ vite.teacher.config.ts
├─ vivliostyle.config.js
├─ index.html
├─ teacher.html
├─ qa/
│  └─ visual-baseline.json
├─ sources/
│  ├─ manifest.json
│  ├─ curriculum/
│  │  └─ bbb-parallel-lines.manifest.json
│  └─ references/
│     └─ core-zaviyot-ben-makbilim.txt
├─ src/
│  ├─ App.tsx
│  ├─ TeacherApp.tsx
│  ├─ main.tsx
│  ├─ teacher-main.tsx
│  ├─ dev/
│  │  └─ DevRefreshBar.tsx
│  ├─ components/
│  │  ├─ A4Page.tsx
│  │  ├─ AnswerArea.tsx
│  │  ├─ ClozeText.tsx
│  │  ├─ ConclusionArrow.tsx
│  │  ├─ DeductionChain.tsx
│  │  ├─ MathText.tsx
│  │  ├─ QuestionBlock.tsx
│  │  └─ ResponseParts.tsx
│  ├─ content/
│  │  ├─ unit-plan.json
│  │  ├─ question-plan.json
│  │  ├─ page-manifest.json
│  │  ├─ booklet-pages.json
│  │  ├─ booklet.ts
│  │  ├─ questions-unit1.ts
│  │  ├─ questions-unit2.ts
│  │  ├─ questions-unit3.ts
│  │  ├─ questions-unit4.ts
│  │  ├─ questions-unit5.ts
│  │  ├─ theorems.ts
│  │  ├─ task-kinds.ts
│  │  ├─ answer-areas.ts
│  │  ├─ page-layout.ts
│  │  ├─ cloze.ts
│  │  ├─ teacher-locator.ts
│  │  └─ answer-key.ts
│  ├─ didactics/
│  │  └─ profile.ts
│  ├─ geometry/
│  │  ├─ core.ts
│  │  ├─ engine.ts
│  │  ├─ primitives.tsx
│  │  ├─ label-font.ts
│  │  ├─ diagram-size.tsx
│  │  ├─ relations.ts
│  │  ├─ angle-roles.ts
│  │  ├─ ParallelLinesDiagram.tsx
│  │  └─ ThreeLinesDiagram.tsx
│  ├─ pages/
│  │  ├─ Unit1Continuation.tsx
│  │  ├─ Unit2Pages.tsx
│  │  ├─ Unit3Pages.tsx
│  │  ├─ Unit4Pages.tsx
│  │  └─ Unit5Pages.tsx
│  └─ styles/
│     ├─ fonts.css
│     ├─ tokens.css
│     ├─ tokens.ts
│     ├─ print.css
│     ├─ teacher-print.css
│     ├─ bbb-source.css
│     ├─ geometry-premium.css
│     ├─ premium-layout.css
│     └─ page-tuning.css
├─ scripts/
│  ├─ prepare-mathjax.mjs
│  ├─ sync-curriculum-source.mjs
│  ├─ render-bbb-curriculum.py
│  ├─ validate-dependency-policy.mjs
│  ├─ validate-schemas.mjs
│  ├─ validate-source-manifest.mjs
│  ├─ validate-curriculum-source-integrity.mjs
│  ├─ validate-curriculum-rendered.mjs
│  ├─ validate-numbering-markers.mjs
│  ├─ validate-question-plan.mjs
│  ├─ validate-forward-progress.mjs
│  ├─ validate-canonical-quality.mjs
│  ├─ validate-page-contract.mjs
│  ├─ validate-visual-baseline.mjs
│  ├─ update-visual-baseline.mjs
│  ├─ lib/
│  │  ├─ booklet-pages.mjs
│  │  ├─ visual-baseline-contract.mjs
│  │  └─ visual-baseline-contract.d.mts
│  ├─ build-pdf.mjs
│  ├─ validate-pdf-crosscheck.mjs
│  ├─ build-teacher-pdf.mjs
│  ├─ write-sbom.mjs
│  └─ write-checksums.mjs
├─ tests/
│  └─ unit/
└─ .github/
   └─ workflows/
      ├─ ci.yml
      └─ release.yml
```

---

# 16. שערי איכות מחייבים

`main` אינו נחשב תקין אם אחד מן השערים הרלוונטיים נכשל:
1. `question-plan` — `npm run validate:question-plan`: סדר השאלות המקוריות ומספרן (`originalTaskCount`, הנעילה היחידה), 8 בלוקי תוכנית הלימודים.
2. `source-manifest` — `npm run validate:sources`: provenance של כל המקורות.
3. `curriculum-source-integrity`
4. `schema-validation` — `npm run validate:schema`: כל המניפסטים מול `booklet-pages.json`.
5. `typecheck`
6. `coverage`
7. `progression`
8. `forward-progress`
9. `local-variety`
10. `numeric-before-algebra`
11. `duplicates`
12. `theorem-conditions`
13. `equation-justification`
14. `student-facing-copy`
15. `continuous-numbering` — רק העמודים ממוספרים ברצף; כל שאלה נפתחת ב־●, כל סעיף ב־•; אין מספרי שאלות, אין אותיות סעיף, אין תווית סוג משימה ואין כותרת נושא בדף התלמיד (תת־השער `markers` ב־`validate:quality` אוכף זאת).
16. `math`
17. `hebrew`
18. `wording-style`
19. `geometry`
20. `page-utilization`
21. `grayscale`
22. `a4`
23. `visual`
24. `geometry-collision`
25. `forced-colors`
26. `visual-baseline`
27. `pdf-dual-engine`
28. `pdf-crosscheck`
29. `teacher-pdf`
30. `deterministic-install`
31. `dependency-policy`
32. `dependency-audit`
33. `no-scheduled-automation`
34. `checksums`
35. `actions-pin`
36. `sbom`
37. `input-fingerprint`
38. `release-contract`
39. `source-provenance`
40. `accessibility`
41. `tagged-pdf`
42. `pdf`
43. `math-island-integrity`
44. `layout-health`
45. `label-line-clearance`
46. `independent-math-verification`
47. `teacher-guide-copy`
48. `answer-areas`
49. `angle-label-placement`
50. `stem-diagram-labels`
51. `diagram-variety`
52. `layout-tokens`
53. `vivliostyle-self-contained`
54. `circle-page-number` — מספר עמוד כמספר-בלבד בעיגול בפינה השמאלית-עליונה; רצף 1..N ללא איפוס; אין „יחידה N”.
55. `curriculum-first` — „שאלות מתוך תוכנית הלימודים” ראשונות; אין „יחידה N” מול התלמיד.
56. `conclusion-arrow` — רכיב ⇓ יחיד ומשותף, כלפי מטה, בטוח ל־RTL ול־PDF.
57. `answer-grid` — משבצות רק באזורי חישוב (עבודה, אלגברה, שתי דרכים, תיקון משוואה). נימוק/הסבר בפרוזה, פסק דין וטבלת ההוכחה הריקה (mode `proof`) — שורות מנוקדות בפסיעת הכתיבה, לא משבצות. אין משבצות במשימות בחירה/מילה אחת.
58. `one-sided-angles` — זוויות חד-צדדיות ל־180°; שמונה הזוויות; הגדרות ישרים/קטעים מקבילים.
59. `parallel-wording` — „בין ישרים מקבילים” תמיד; „בין מקבילים” אינו מוצג לתלמיד.
60. `page-contract` — `npm run validate:pages`: כל עמוד ב־`booklet-pages.json` רשום, תוכנית הלימודים ראשונה, המניפסט הפנימי תואם.
61. `curriculum-rendered` — `npm run validate:curriculum:rendered`: הרינדור של בלוקי תוכנית הלימודים זהה למקור (למעט הנרמול המותר 8.4).
62. `svg-geometry` — `npm run validate:quality`: כל עמודי הנושאים הדידקטיים מרנדרים שרטוט SVG וקטורי (10, 10.5).
63. `single-source` — `tests/unit/single-source.test.ts`: סדר הרינדור, עמוד כל משימה וסדר תוכנית הלימודים נגזרים ממקור אחד.

Validators של מקוריות/הדרגתיות/מספור/פורמט פועלים על הנושאים הדידקטיים (2–9). `curriculum-source-integrity` פועל על בלוק תוכנית הלימודים ומוודא שאין שינוי בתוכן המקור, למעט נורמליזציית התצוגה „בין מקבילים” ← „בין ישרים מקבילים”; סימן ה־● בראש כל שאלה ומספר העמוד הם chrome ואינם שינוי תוכן.

---

# 17. שיטת העבודה לפי סדר ביצוע

1. לאנדקס ולסווג את המקורות הרלוונטיים.
2. להגדיר את הנושא ואת מטרותיו.
3. בנושאים הדידקטיים: לתכנן משימות ו־fingerprints לפי סדר הקושי.
4. בנושאים הדידקטיים: לבדוק forward progression ולמנוע חזרות.
5. בנושאים הדידקטיים: לכתוב ניסוחים בהתאם ל־Corpus.
6. בנושאים הדידקטיים: לאמת מתמטיקה ופתרונות.
7. בנושאים הדידקטיים: ליצור שרטוטי SVG בהתאם ל־visualDemand.
8. בבלוק תוכנית הלימודים: לשלוף רק את הבלוקים המופיעים ב־manifest ולשמר אותם ללא שינוי.
9. לבצע עימוד A4, מספור עמודים רציף בעיגול וניצול שטח.
10. להריץ Hebrew/Bidi/student-facing QA; בבלוק תוכנית הלימודים לא לבצע rewrite.
11. להריץ geometry/visual/grayscale QA.
12. להריץ `curriculum-source-integrity` על בלוק תוכנית הלימודים.
13. להפיק PDF ולבצע cross-check.
14. רק לאחר מעבר QA להרחיב לעמוד הבא או לנושא הבא.

---

# 18. קריטריון סיום

הפרויקט מוכן רק כאשר:
- החוברת מאורגנת לפי נושאים טבעיים, כאשר „שאלות מתוך תוכנית הלימודים” פותחות אותה ואחריהן הנושאים הדידקטיים לפי סדר הלמידה (כותרות הנושא הן מבנה פנימי לסידור ולמדריך המורה; הן אינן מודפסות בדף התלמיד) (הגדרות ושמונה הזוויות → זוויות מתאימות ומתחלפות → המשפטים הישירים → חישובי זוויות → חישוב זוויות עם נעלם → יישומים → נימוק והוכחה → המשפטים ההפוכים); „יחידה N” אינו מופיע מול התלמיד.
- מספור עמודים רציף יחיד 1..N ללא איפוס, מוצג כמספר-בלבד בעיגול בפינה השמאלית-עליונה. **רק העמודים ממוספרים.**
- כל שאלה נפתחת ב־● (נקודה שחורה גדולה); כל סעיף ב־• (נקודה קטנה יותר). אין מספרי שאלות, אין אותיות סעיף, אין תוויות סוג משימה ואין כותרת נושא בדף התלמיד (כלל הברזל „אין דמו”).
- בבלוק תוכנית הלימודים נשמר מבנה המקור הפנימי כתוכן, בעוד סימן ה־● בראש השאלה ומספר העמוד הם chrome; „בין מקבילים” מוצג תמיד כ„בין ישרים מקבילים”, והמקור נשמר ב־provenance.
- נלמדות הגדרות ישרים מקבילים וקטעים מקבילים, שמונה הזוויות שיוצר חותך, וזוויות חד-צדדיות המשלימות ל־180°.
- המשפטים הישירים נלמדים בניסוח מלא „בין ישרים מקבילים” עם תנאי המקבילות.
- קיימות משימות המתקנות את הטעות “זוויות מתחלפות/מתאימות שוות” ללא תנאי, כולל שרטוטים שבהם הישרים אינם מקבילים.
- „מתאימות/מתחלפות” נלמדות קודם כל כקשר מיקומי, לא כשוויון אוטומטי.
- מספרים קודמים לאלגברה בכל מיומנות דידקטית.
- כל משימה אלגברית מסתיימת בזווית במעלות (°), לא ב־x בלבד; זרם: זיהוי → משפט → משוואה → x → הצבה → זווית; אין סוגריים דקורטיביים.
- קיים רכיב ⇓ מסקנה יחיד, זהה, בטוח ל־RTL ול־PDF, המשמש בהיסקים המדורגים.
- המשפטים ההפוכים נלמדים עם פיגום מדורג (השלמת מילה/שורה → דוגמה מספרית קלה → מתמטיקה חלקית → ⇓ → מסקנת מקבילות), כולל דוגמת 112° מודרכת עם נימוק הפוך נכון מתמטית; אין „הוכח מאפס” בשלב מוקדם.
- בחירה מרובה כוללת גם בחירת המשפט/הטענה הנכונים, כשהמסיחים הם טעויות תלמיד אמיתיות.
- אזורי משבצות (דף חשבון) מופיעים רק היכן שנדרשת עבודה אמיתית; משימות זיהוי/בחירה/סימון/מילה אחת אינן מקבלות רשת גדולה; גובה אזור העבודה תואם את העבודה.
- אין כותרת/תווית/סימון בשרטוט החושפים את סוג הזוויות כאשר זיהוי הסוג הוא המיומנות הנבדקת.
- אין שאלות או סעיפים מקוריים שחוזרים מכנית באותה רמה.
- השרטוטים מדויקים ברמת מד־זווית: הזווית המצוירת שווה למספר הכתוב; המקבילות אמיתית; השרטוטים מגוונים בהדרגה.
- התמונות הקיימות המושתלות יפה לא שונו.
- אין placeholder, demo copy, טקסט פיתוח או הסברים שאינם מיועדים לתלמיד.
- כל עמודי A4 מנוצלים באופן מאוזן; העיצוב אחיד לחלוטין בכל הפרויקט ללא CSS טלאי לעמוד יחיד.
- הצבעים משמשים דידקטית בלבד וכל עמוד עובר grayscale QA.
- כל 8 בלוקי שאלות תוכנית הלימודים שאותרו ב־`bbb` נכללים ונשמרים ללא שינוי תוכני (למעט נורמליזציית „בין ישרים מקבילים”).
- קיים מפתח תשובות מלא (62 תשובות) לנושאים הדידקטיים, מאותר לפי מספר העמוד ומיקום השאלה בעמוד, ובבלוק תוכנית הלימודים נשמרת מדיניות המקור כפי שהיא.
- קיים PDF תלמיד A4 קנוני ו־PDF Chromium עצמאי עם cross-check על אותו מספר עמודים N.
- קיים PDF מורה A4 נפרד, מבודד מ־Pages, שמופק ישירות ממפתח התשובות.
- קיים visual baseline קנוני לכל 21 דפי התלמיד, מאושר מחדש לאחר המבנה החדש.
- כל PDF מקבל SHA-256 ו־build manifest; התקנת CI דטרמיניסטית; Release מתג עובר את אותם שערים.
- כל שערי האיכות הרלוונטיים עוברים.

---

# 19. עקרון הכרעה סופי

כאשר יש בחירה בין פתרון קל יותר לפיתוח לבין פתרון מדויק, מקצועי ומועיל יותר לתלמיד — יש לבחור בפתרון המקצועי, כל עוד הוא בר־תחזוקה וניתן לבדיקה אוטומטית. בבלוק תוכנית הלימודים, נאמנות למקור היא דרישה עליונה ואסור “לשפר” את שאלת המקור על חשבון נאמנות.