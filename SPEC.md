# SPEC — מקור האמת היחיד של Makbilim

> מסמך זה הוא החוזה הקנוני היחיד של הפרויקט. כל קוד, תוכן, JSON, workflow, preview, PDF ובדיקה חייבים לממש אותו. במקרה של סתירה — `SPEC.md` גובר. דרישה חדשה נכנסת קודם לכאן בניסוח מקצועי ותפעולי ורק לאחר מכן מיושמת.

---

# 1. מטרת המוצר

**שם הפרויקט:** זוויות בין ישרים מקבילים

יש ליצור חוברת לימוד והערכה לחטיבת הביניים ברמת ספר לימוד מקצועי: מדויקת מתמטית, מדורגת דידקטית, כתובה בעברית תקינה, מעוצבת באחידות גבוהה, מבוססת שרטוטים וקטוריים, ומוכנה להדפסה איכותית בצבע ובשחור־לבן.

התוצר אינו דמו, אינו אוסף תרגילים גנרי ואינו אמור להיראות או להישמע כמו פלט AI.

---

# 2. פרוטוקול פירוש דרישות לסוכני AI

## MUST
- לתרגם כל דרישת משתמש לדרישה תפעולית מקצועית, ולא לשמור תמלול שיחה.
- לשמר את כוונת המשתמש במלואה תוך ניסוח שניתן ליישום ולבדיקה.
- לציין, לפי הצורך: מטרת הדרישה, התנהגות נדרשת, התנהגות אסורה, קריטריון קבלה והשפעה על QA.
- להשתמש ב־`SPEC.md` כמקור הדרישות היחיד.

## MUST NOT
- לא להוסיף דרישה חדשה בקובץ אחר בלי לעדכן קודם את `SPEC.md`.
- לא לנחש פרט מהותי שאינו ניתן להסקה בבטחה מן ה־SPEC, מקובצי המקור או מן המימוש הקיים.

## WHEN UNCERTAIN
כאשר אי־ודאות מהותית עשויה לשנות את התוכן, הדידקטיקה, הניסוח או העיצוב — יש לבקש החלטה מהמשתמש.

---

# 3. Corpus המקורות וכלל השימוש בו

קובצי המקור שסופקו או נאספו לפרויקט הם **Corpus ייחוס פדגוגי ולשוני**.

הם משמשים ללמידת:
- סוגי שאלות מקובלים.
- פעלי הוראה וניסוח הוראות.
- אורך וקצב של שאלה.
- רמת פירוט של נתונים.
- מבני שרטוט.
- רצף קושי.
- סוג התשובה המצופה.

## MUST
- לפני כתיבת גל שאלות חדש, לסקור את המקורות הרלוונטיים ליחידה.
- לשמור לכל משימה `sourceArchetypeRefs` פנימי.
- לנסח שאלות בשפה המקובלת בדפי עבודה ובמבחנים בישראל.

## MUST NOT
- אין להעתיק שאלה חיצונית בשלמותה.
- אין ליצור העתקה מוסווית באמצעות החלפת מספרים/אותיות בלבד.
- אין להעתיק שרטוט חיצוני באופן זהה כאשר ניתן ליצור תצורה מקורית.

כאשר חומר הוא חומר שהמשתמש רשאי לעבד, מותר לבצע adaptation משמעותי. כאשר המקור חיצוני, לומדים את הארכיטיפ, ה־register והמיומנות ויוצרים תוכן מקורי.

מבנה המקורות:

```text
sources/
├─ originals/
├─ references/
└─ manifest.json
```

`SPEC.md` נשאר מקור האמת היחיד לדרישות; `sources/` הוא corpus ייחוס בלבד.

---

# 4. המשפטים הקנוניים

## 4.1 משפטים ישירים
1. **אם שני ישרים מקבילים נחתכים על ידי ישר שלישי, אז כל זוג זוויות מתאימות שוות זו לזו.**
2. **אם שני ישרים מקבילים נחתכים על ידי ישר שלישי, אז כל זוג זוויות מתחלפות שוות זו לזו.**

## 4.2 משפטים הפוכים
3. **אם שני ישרים נחתכים על ידי ישר שלישי, וזוג זוויות מתאימות שוות זו לזו, אז שני הישרים מקבילים.**
4. **אם שני ישרים נחתכים על ידי ישר שלישי, וזוג זוויות מתחלפות שוות זו לזו, אז שני הישרים מקבילים.**

## 4.3 הבחנה מושגית מחייבת
- הסיווג “מתאימות/מתחלפות” מתאר מיקום ואינו מבטיח שוויון.
- “זוויות מתחלפות שוות” אינו משפט תקף ללא תנאי המקבילות.
- “זוויות מתאימות שוות” אינו משפט תקף ללא תנאי המקבילות.
- החוברת חייבת לכלול משימות שמאתרות ומתקנות את הטעות הזאת.

---

# 5. מבנה התוכן — יחידות ממוספרות

היחידות והעמודים ממוספרים; **השאלות עצמן אינן ממוספרות**. כל שאלה מתחילה ב־**● שחור**.

תכנון בסיס: 7 יחידות, כ־14 עמודי תלמיד A4 וכ־58 משימות ייחודיות. איכות, הדרגתיות וניצול עמוד גוברים על יעד מספרי קשיח.

## יחידה 1 — מושגים בסיסיים
מטרה: זיהוי חזותי והבנת המונחים לפני חישוב.

MUST לכלול:
- זיהוי שני ישרים וישר חותך.
- זיהוי זוג מתאימות וזוג מתחלפות.
- סימון הזווית המתאימה/המתחלפת לזווית נתונה.
- מיון זוגות: מתאימות / מתחלפות / אחר.
- תצורות אופקיות, משופעות ומסובבות.
- הבנה שסיווג הזוג אינו תלוי בכך שהישרים מקבילים.

רמה: D1 → D2.

## יחידה 2 — המשפטים הישירים ותנאי המקבילות
מטרה: שליטה בניסוח המדויק של המשפטים.

MUST לכלול:
- השלמת משפטים.
- השלמת מילים כגון “מקבילים”, “מתאימות”, “מתחלפות”, “שוות”.
- הבחנה בין ניסוח מדויק לניסוח חסר.
- נכון/לא נכון על תנאי המקבילות.
- ניתוח טענות תלמידים כאשר אחת משמיטה תנאי הכרחי.

רמה: D1 → D2.

## יחידה 3 — חישוב ישיר במספרים
מטרה: יישום חד־שלבי של המשפטים הישירים.

MUST:
- לעבוד במספרים בלבד.
- לכלול מתאימות ומתחלפות.
- לכלול בחירת משפט בחלק מן המשימות.

רמה: D1 → D2.

## יחידה 4 — חישוב משולב במספרים
מטרה: שרשרת קצרה של שני קשרים.

MUST לכלול:
- מתאימות/מתחלפות + קודקודיות.
- מתאימות/מתחלפות + צמודות/זווית שטוחה.
- שני שלבי הסקה.
- בחירת מסלול או נתון עודף בחלק מן המשימות.

רמה: D2 → D3.

## יחידה 5 — טבלאות, ייצוגים ואלגברה
מטרה: מעבר הדרגתי מייצוג מספרי לאלגברי.

סדר פנימי מחייב:
1. טבלאות עם מספרים.
2. שליפה והסקה מטבלה.
3. התאמת זווית לערך.
4. משוואה פשוטה עם `x`.
5. ביטויים אלגבריים בשתי זוויות.
6. מציאת `x` ולאחר מכן חישוב זווית.

**אין להציג אלגברה לפני שהתלמיד תרגל את אותה מיומנות במספרים.**

רמה: D2 → D4.

## יחידה 6 — נימוק, בחירת משפט והערכת נתונים
MUST לכלול:
- קביעה ונימוק.
- בחירת המשפט המתאים.
- “האם ניתן לקבוע?”.
- מידע מספיק / לא מספיק.
- איתור ותיקון טעות.
- ניתוח טענת תלמיד.
- הוכחה קצרה כאשר היא מתאימה לרמת חטיבת ביניים.

רמה: D3 → D5.

## יחידה 7 — משפטים הפוכים
יחידה מתקדמת בלבד.

MUST לכלול:
- השלמת שני המשפטים ההפוכים.
- הבחנה בין משפט ישיר למשפט הפוך.
- נתון שוויון זוויות → הסקת מקבילות.
- בדיקת מספיקות נתונים.
- קביעה אילו ישרים מקבילים ונימוק.
- מציאת ערך מספרי, ובהמשך `x`, שיבטיח מקבילות.
- נכון/לא נכון על המשפט ההפוך.
- הוכחה קצרה לקביעת מקבילות.

רמה: D3 → D5.

---

# 6. מודל הקושי וההדרגה

- **D1 — זיהוי ושליפה:** סימון, מיון, השלמת מילה, בחירה פשוטה.
- **D2 — יישום חד־שלבי:** משפט אחד, חישוב ישיר, נכון/לא נכון בסיסי.
- **D3 — שני שלבי הסקה:** שילוב קשרים, טבלה, התחלת אלגברה, שני חותכים פשוטים.
- **D4 — בחירת דרך ומידע מורכב:** אלגברה, סינון נתונים, בחירת נימוק, התחלת משפטים הפוכים.
- **D5 — נימוק והוכחה:** איתור טעות, הוכחה קצרה, שילוב משפט ישיר והפוך, תכנון פתרון.

סדר התקדמות מחייב:

**זיהוי → סיווג → התאמה → ניסוח משפט → תנאי מקבילות → חישוב מספרי חד־שלבי → חישוב מספרי דו־שלבי → טבלה/ייצוג → אלגברה → נימוק → מספיקות נתונים → הוכחה → משפטים הפוכים.**

`x` או ביטויים אלגבריים אינם מופיעים לפני תרגול מספרי מקביל.

---

# 7. גיוון הוראתי

החוברת משלבת לפי הצורך:
- זיהוי וסימון.
- התאמה.
- השלמת משפט.
- השלמת טבלה ושליפה ממנה.
- חישוב זווית חסרה.
- בחירה מרובה.
- נכון/לא נכון.
- נכון/לא נכון + נימוק.
- קביעה ונימוק.
- בחירת משפט.
- חישוב משתנה.
- ניתוח טענת תלמיד.
- מידע מספיק/לא מספיק.
- איתור טעות.
- הוכחה קצרה.
- משימת העברה בתצורה לא שגרתית.

גיוון הוא אמצעי דידקטי ולא יעד אסתטי. אין להכניס פורמט חריג שאינו מקובל בדפי עבודה רק לשם גיוון.

מסיחים בשאלות אמריקאיות חייבים לנבוע מתפיסות שגויות סבירות.

---

# 8. חוזה ניסוח בעברית

השפה לתלמיד חייבת להיות קצרה, ישירה, פורמלית וטבעית, ברמת דף עבודה או מבחן ישראלי.

פעלי הוראה מועדפים: `חשבו`, `מצאו`, `קבעו`, `נמקו`, `השלימו`, `סמנו`, `בחרו`, `רשמו`, `כתבו`, `התאימו`.

ארכיטיפים מקובלים:
- “כתבו בכל סעיף האם …”
- “השלימו את המשפט.”
- “בחרו את התשובה הנכונה.”
- “קבעו אם הטענה נכונה או לא נכונה. נמקו.”
- “סמנו בשרטוט זוג זוויות …”
- “חשבו את ערכו של …”
- “השלימו את הטבלה.”
- “על סמך הנתונים, קבעו אם הישרים מקבילים. נמקו.”

MUST NOT:
- ניסוחי AI שיחתיים כגון “בואו נגלה”, “מה אתם חושבים?”, “אתגר מגניב”.
- סיפור מסגרת מיותר.
- תרגום מילולי מאנגלית.
- הסבר־יתר שמסגיר דרך פתרון.

כל שאלה עוברת `wording-style` QA.

---

# 9. עברית, Bidi וכתיב מתמטי

- RTL מלא וטבעי.
- התאמה נכונה של זכר/נקבה ויחיד/רבים.
- פיסוק עברי תקין.
- אין רווחים כפולים או רווח לפני סימני פיסוק.
- סימן מעלה נצמד לערך: `64°`.
- מינוח אחיד: “זוויות מתאימות”, “זוויות מתחלפות”, “ישר חותך”, “ישרים מקבילים”.
- מתמטיקה, אותיות לטיניות ומספרים בתוך עברית מבודדים LTR במנגנון תקני.
- מנוע מתמטי: **MathJax 4.x**; SVG output מועדף להדפסה כאשר הוא נותן תוצאה מיטבית.
- סימון אחיד: `∠ABC`, `AB ∥ CD`, `x`, `α`, `β`, `γ`, `180°`.

---

# 10. מקוריות ומניעת כפילויות

לכל משימה נשמרת טביעת אצבע הכוללת לפחות:
- מיומנות.
- משפט נדרש.
- טופולוגיית שרטוט.
- סוג נתון.
- סוג נדרש.
- מספר שלבי הסקה.
- סוג תשובה.
- רמת קושי.
- משפחת ערכים.
- אותיות/סמלים.
- `wordingArchetype`.
- `instructionVerb`.
- `misconceptionTarget`.
- `sourceArchetypeRefs`.
- רמת העברה.

שתי שאלות נחשבות דומות מדי אם נתיב הפתרון והמבנה כמעט זהים גם כאשר המספרים או האותיות שונים.

---

# 11. פרופיל דידקטי לכל משימה

כל משימה MUST לכלול:

`id`, `unit`, `page`, `difficulty`, `learningObjective`, `assessmentPurpose`, `prerequisites`, `theoremIds`, `format`, `responseMode`, `reasoningSteps`, `misconceptionTarget`, `languageLoad`, `visualLoad`, `transferLevel`, `evidenceOfLearning`, `wordingArchetype`, `instructionVerb`, `sourceArchetypeRefs`, `numericBeforeAlgebra`, `fingerprint`.

אין שאלה ללא פרופיל מלא.

---

# 12. חוזה עיצוב והדפסה

## 12.1 A4 וניצול שטח

- כל עמוד הוא **A4 מדויק: 210×297 מ״מ**.
- כל שטח העבודה הזמין חייב להיות מנוצל באופן מאוזן.
- אסור להשאיר שליש עליון, אמצעי או תחתון ריק ללא הצדקה פדגוגית.
- רווח לבן מותר רק כאשר הוא משרת קריאות, הפרדה, אזור תשובה או היררכיה חזותית.
- כאשר נותר חלל גדול, המערכת מבצעת reflow: הרחבת אזור תשובה, הגדלת שרטוט באופן סביר, שינוי פריסה או העברת משימה מתאימה.
- אסור למלא חלל באמצעות מלל דמו או קישוט חסר ערך.
- אין לפתור עומס באמצעות הקטנת טקסט מתחת לסף קריאות.

Acceptance gate: `page-utilization` חייב לעבור בכל עמוד.

## 12.2 אחידות ברמת ספר לימוד

- מערכת טיפוגרפית אחת לכל החוברת.
- design tokens משותפים לכותרות, גוף, נוסחאות, תוויות, קווים, קשתות, צבעים, תיבות וטבלאות.
- כל השרטוטים חייבים להיראות כאילו הופקו על ידי אותו מאייר מתמטי ובאותו ספר.
- אין פונט או סגנון אד־הוק בעמוד בודד ללא סיבה מוכחת.

## 12.3 צבע — פונקציה דידקטית בלבד

צבע מותר כאשר הוא מסייע להבנה, לדוגמה:
- הדגשת זוג זוויות.
- הבחנה בין שני חותכים.
- הבחנה בין נתון לנדרש.
- קידוד עקבי בתוך שאלה כאשר הדבר אינו מסגיר את הפתרון.

MUST NOT:
- צבע לצורך קישוט בלבד.
- מידע חיוני שעובר באמצעות צבע בלבד.
- מערכת צבעים משתנה ולא עקבית בין עמודים.

## 12.4 שחור־לבן

כל עמוד חייב להישאר ברור ושימושי בהדפסת grayscale. מידע חיוני מובחן גם באמצעות צורה, סוג קו, עובי, תווית או מיקום.

Acceptance gate: `grayscale` חייב לעבור לכל עמוד.

## 12.5 מבנה שאלה

- שאלות אינן ממוספרות.
- כל שאלה מתחילה ב־● שחור.
- אזור תשובה מותאם לסוג המשימה.
- אין clipping, overflow או פיצול שפוגע בהבנה.
- העימוד חייב להיראות כמו ספר לימוד, לא כמו Word ולא כמו dashboard/web app.

## 12.6 תחתית קנונית

בכל דף תלמיד מופיע footer אחיד בשתי שורות:

**יניב רז - מדריך מחוזי חט"ב בעיר ירושלים**

**הדרכה במחוז ירושלים והעיר ירושלים - מנח"י, בהובלת איילת קריספין**

ה־footer עדין, אחיד, אינו מתחרה בתוכן ואינו מצמצם באופן מהותי את שטח העבודה.

---

# 13. גרפיקה ושרטוטים

כל שרטוט גאומטרי שניתן ליצור וקטורית MUST להיווצר כ־**SVG**.

מנוע הגאומטריה הפרמטרי צריך לתמוך ב:
- Point / Vector / Line / Segment.
- זוגות ישרים מקבילים.
- חותך אחד או יותר.
- נקודות חיתוך.
- זוויות וקשתות זווית.
- סימני מקבילות.
- תוויות נקודות/ישרים.
- משתנים ומעלות.
- מיקום אוטומטי של תוויות.
- collision detection.
- minimum spacing.
- auto-fit / auto-scale / viewBox.
- תצורות אופקיות, משופעות ואנכיות.
- חותך בשיפוע חיובי/שלילי.
- שני חותכים.

MUST NOT:
- תווית על קו.
- קשת שחוצה טקסט.
- שרטוט שמטעה לגבי זווית חדה/קהה או יחס בסיסי.

---

# 14. טכנולוגיות

העיקרון: להשתמש בטכנולוגיה החזקה והמתאימה ביותר כאשר היא משפרת דיוק, איכות, אחידות, נגישות או בדיקות.

- **TypeScript strict** — מודל נתונים, generator, validators.
- **React + Vite** — preview וקומפוזיציית עמודים.
- **SVG** — שרטוטים.
- **MathJax 4** — מתמטיקה.
- **Vivliostyle + CSS Paged Media** — A4/PDF.
- **Zod** — schema validation.
- **Vitest** — unit/integration tests.
- **Playwright** — browser, print, screenshots, visual regression, grayscale QA.
- **Python + SymPy** — אימות אלגברי/סמלי עצמאי.
- **Python analytics** — קושי, כיסוי, מספרים, אותיות ודמיון.
- **Git + GitHub** — versioning.
- **GitHub Actions** — CI.

---

# 15. אימות מתמטי

כל שאלה חישובית חייבת להיפתר גם על ידי מנגנון אימות שאינו תלוי בקוד התצוגה.

MUST לבדוק:
- עקביות בין שרטוט לנתונים.
- תשובה יחידה כאשר מצופה תשובה יחידה.
- זוויות בטווח תקין.
- אלגברה ללא פתרון שגוי/דו־משמעי.
- התאמה בין המשפט לנתונים.
- אין הסקת שוויון ממתאימות/מתחלפות ללא תנאי המקבילות.
- במשפט הפוך, הנתון אכן מספיק לקביעת מקבילות.

---

# 16. ארכיטקטורת הריפו

```text
Makbilim/
├─ SPEC.md
├─ README.md
├─ sources/
│  ├─ originals/
│  ├─ references/
│  └─ manifest.json
├─ package.json
├─ tsconfig.json
├─ vite.config.ts
├─ vitest.config.ts
├─ playwright.config.ts
├─ index.html
├─ src/
│  ├─ app/
│  ├─ domain/
│  ├─ content/
│  │  ├─ unit-plan.json
│  │  ├─ question-plan.json
│  │  ├─ questions.ts
│  │  ├─ theorems.ts
│  │  └─ answer-key.ts
│  ├─ didactics/
│  │  ├─ difficulty.ts
│  │  ├─ coverage.ts
│  │  ├─ fingerprint.ts
│  │  ├─ misconceptions.ts
│  │  └─ wording.ts
│  ├─ math/
│  ├─ geometry/
│  ├─ components/
│  ├─ pages/
│  └─ styles/
├─ python/
│  └─ validate_math.py
├─ scripts/
│  ├─ validate-single-source.mjs
│  ├─ validate-source-corpus.mjs
│  ├─ validate-question-plan.mjs
│  ├─ validate-duplicates.mjs
│  ├─ validate-didactics.mjs
│  ├─ validate-progression.mjs
│  ├─ validate-numeric-before-algebra.mjs
│  ├─ validate-theorem-conditions.mjs
│  ├─ validate-no-demo-copy.mjs
│  ├─ validate-hebrew.mjs
│  ├─ validate-wording-style.mjs
│  ├─ validate-math.mjs
│  ├─ validate-svg.mjs
│  ├─ validate-page-utilization.mjs
│  ├─ validate-grayscale.mjs
│  ├─ validate-pages.mjs
│  ├─ build-pdf.mjs
│  └─ crosscheck-pdf.mjs
├─ tests/
│  ├─ unit/
│  ├─ visual/
│  └─ e2e/
├─ artifacts/
│  ├─ pdf/
│  ├─ screenshots/
│  └─ reports/
└─ .github/workflows/ci.yml
```

---

# 17. שערי איכות

`main` אינו נחשב תקין אם אחד מאלה נכשל:

1. `spec:guard`
2. `source-corpus`
3. `typecheck`
4. `schema`
5. `coverage`
6. `progression`
7. `numeric-before-algebra`
8. `duplicates`
9. `theorem-conditions`
10. `no-demo-copy`
11. `math`
12. `hebrew`
13. `wording-style`
14. `geometry`
15. `page-utilization`
16. `grayscale`
17. `a4`
18. `visual`
19. `pdf`

---

# 18. שיטת עבודה

עובדים בגלים:

**סקירת מקורות היחידה → תכנון ארכיטיפים → כתיבת שאלות מקוריות → QA מתמטי → QA דידקטי → QA ניסוח ועברית → QA גרפי → QA הדפסה → תיקון → הרחבה.**

אין לייצר את כל החוברת ללא QA ביניים.

---

# 19. קריטריון סיום

הפרויקט נחשב מוכן רק כאשר:
- כל היחידות בנויות לפי הסדר הדידקטי.
- תחילת החוברת מבוססת על זיהוי והבנת מושגים.
- המשפטים הישירים נלמדים בניסוח מלא עם תנאי המקבילות.
- קיימות שאלות שמתקנות את התפיסה “זוויות מתחלפות שוות” ללא תנאי.
- מספרים קודמים לאלגברה בכל מיומנות.
- יחידת המשפטים ההפוכים מופיעה בשלב המתקדם בלבד.
- כל השאלות מקוריות ברמת המבנה ולא רק במספרים/אותיות.
- אין placeholder, demo copy או מלל פיתוח בדפי התלמיד.
- כל 14 עמודי A4 מנוצלים באופן מאוזן.
- כל הדפים עוברים grayscale QA.
- כל הפונטים והשרטוטים אחידים ברמת ספר לימוד.
- כל השרטוטים SVG ועברו geometry/collision/visual QA.
- קיים מפתח תשובות.
- קיים PDF A4 סופי תקין.
- כל שערי האיכות עוברים.

---

# 20. עקרון הכרעה

כאשר יש בחירה בין פתרון קל יותר לפיתוח לבין פתרון מדויק, מקצועי ומועיל יותר לתלמיד — יש לבחור בפתרון המקצועי, כל עוד הוא בר־תחזוקה וניתן לבדיקה אוטומטית.
